# v0.8.0 局内随机事件 专项审核报告

- 审核员：`expert-agent-planner-gpt55`（GPT 系列；与执行方 Claude 系列 lead-game-programmer / senior-gameplay-engineer / senior-systems-engineer 基底不同）
- 审核时间：2026-05-23
- 审核范围：commits `c16ab55`（T1+T2 框架+NPC）+ `b6c5822`（T3 5 种事件）+ `38ee206`（T4+T5 HUD+清理）
- HEAD：`38ee206`，基线对比：v0.7.0 发布 tag `8910606`
- 上游输入：方向 `a9d9199` / 看板 `03e213f` / ADR `d7a4e64` / 数值 `17359d7`
- 审核方法：`git show` + `git diff` + Read + Grep；未做浏览器实机测试；未修改任何代码 / 文档
- **审核结论：⚠️ 有条件通过**
  - 无 S0 / S1 阻塞性问题
  - 立项文档 / ADR / 不回潮检查 / 越界检查全部通过
  - 7 项 S2 / S3 数值层偏离需 PM 节点 D 实机验证后由制作人决定是否接受

## §1. 立项文档覆盖率

13 项必做范围 100% 落地：
- 工程基础（ENCOUNTER_TYPES 注册表 + 5 种事件 + state.run.encounters + state.run.encounterCooldownThisGalaxy + notifyEncounters + applyEncounterReward + seedEncountersForLevel + 独立 RNG + nextLevel 集成 + 8 处 notifyObjective 追加）
- NPC 系统扩展（npc.kind 4 种 + npc.role + npc.encounterId + tickNpc 分支 + damageNpc 派发分流 + buildNpcHudAlerts role 分组）
- HUD 与视觉反馈（buildEncounterHudAlerts + showHostileStationAlert priority 队列 + fragment origin derelict/signal + CSS 7 类）
- 战斗与拾荒接入（spawnEnemy("pirate") + makeWreckFragment origin 扩展）
- 5 种事件类型完整（make / tick / onXxx / isComplete / isFailed / getDetail / getHint / render 全部）

## §2. 数值方案对照

~90% 一致，主要偏离：
- ⚠️ S2-1：trader 实际离场时间 ~15s 而非 30s（NPC 1200px / 80 px/s ≈ 15s 提前到达 dest）
- ⚠️ S2-2：触发延迟按抽样顺序（base + i × stagger）而非事件类型（spec 60/90/120/150/180s 按类型）
- ⚠️ S2-3：ambush > 700px 直接 expired（spec 期望保持 active）
- ⚠️ S3：distress / ambush 海盗数量 uniform 而非 level 曲线
- ⚠️ S3：ambush 海盗 HP ×1.1 未实现
- ⚠️ S3：derelict wreck 数量 level 2/4 偏 +1
- ⚠️ S3：HUD_PRIORITY_* 常量未命名
- ⚠️ S3：trader 资源不足无 toast 反馈

22 项数值与 spec 一致：trader / distress / ambush / derelict / signal spawn 距离 / NPC HP / 奖励 / 冷却 / priority / alert 持续时间 / 并发上限 / 关卡权重比例 等。

## §3. ADR 对照

9 项关键决定 100% 落地：
- §1 ENCOUNTER_TYPE_DEFAULTS 独立字段集
- §2 state.run.encounters 独立结构
- §3 updateEncounters(dt) 独立函数（在 updateObjective 之后 updateCamera 之前 ✓）
- §4 5 种 spawn 距离
- §5 npc.role + npc.encounterId 扁平字段
- §6 state.hudCenterAlertQueue priority 队列
- §7 nextLevel 完全清空
- §8 fragment.origin 扩展
- §9 ENCOUNTER_MAX_CONCURRENT=3 + 独立 RNG 流

T1-T5 拆任务全部完成（c16ab55 + b6c5822 + 38ee206 三个 commit）。

## §4. 不回潮检查

v0.1.x-v0.7.0 全部关键路径完整保留：
- 5 种 enemy kind 行为不变
- v0.7.0 锁敌（priorityTarget + selectTarget + F 键齐射 + salvoSize）完全等价
- v0.6.0 hostile-station BOSS 战平衡完全保留
- v0.5.0 研发系统 / getCellStat / cell.tier / cell.mod 完全等价
- v0.4.0 8 种 OBJECTIVE_TYPES 不破坏（含 escort 任务 NPC 行为）
- v0.3.0 fragment / wreck / enemy-wreck / salvage 路径不破坏
- 8 处 notifyObjective 完全保留（仅追加 notifyEncounters）
- grantObjectiveReward / applyEncounterReward 完全独立
- 现有按键完全保留（Space / Enter / WASD / 1-9 / 0 / - / = / Escape / F / [ ] / 左键 / 拖动旋转）+ 新增 T/Y 仅在 trader.playerInTradeRange 时生效
- SAVE_KEY 未变；旧存档兼容（state.run.encounters / encounterCooldownThisGalaxy / hudCenterAlertQueue 缺失时默认）
- `__gameTest.findEscortSeed / getObjectiveType / resetRun` 完全等价

## §5. 边界 / 异常情况

核心边界全处理（20 项验证通过）：
- 玩家不参与任何事件时游戏体验等价 v0.7.0
- seedEncountersForLevel(level=6) 返回空（终末关无事件）
- seedEncountersForLevel(level=0) 仅 trader
- 多事件并发 HUD priority 队列正确抢占（队列上限 5）
- 同 priority FIFO（enqueuedAt 升序）
- distress 友方 NPC 死亡 → encounter.failed = true → 无奖励
- ambush 战斗胜利后 spawn 3 enemy-wreck
- derelict / signal 关内永久（仅 nextLevel 清空）
- showHostileStationAlert 旧 3 参调用兼容（默认 priority=100）
- 玩家 turret 不误击 trader / distress-pilot 友方（NPC 不在 state.enemies 中）
- 玩家 v0.7.0 priorityTarget 仅锁 state.enemies，不影响事件 NPC
- spawn 失败时 encounter 自动 expired

2 项 S3 偏离：
- ambush 完成后 NPC 残留（state="complete" 不触发 expireEncounter）
- pickEncounterSpawnPos 缺与现有实体的 minDist 避让

## §6. 越界检查

`git diff ad8799c..38ee206 --stat` 实测：仅 6 个允许的文件
- Game/game.js（修改 +1144 -26）
- Game/style.css（修改 +62）
- Workspaces/v0.8.0-* 4 份新文档

未触碰 Game/index.html、Docs/、.cursor/、42.md、持续开发Prompt.md、.gitignore、Releases/、其他版本看板。✓ 完全通过。

## §7. 残留风险评估

- **S0 阻塞：0 项**
- **S1 影响接受：0 项**
- **S2 体验下降：3 项**
  - S2-1：trader 实际 15s 离场（spec 30s），玩家挫败感强
  - S2-2：触发延迟按抽样顺序而非类型曲线，节奏与设计不一致
  - S2-3：ambush > 700px 即 expired，不能"飞近观察再退回"
- **S3 后续优化：10 项**
  - S3-1：HUD_PRIORITY_* 常量未命名
  - S3-2/3：distress / ambush 海盗数量 uniform 而非 level 曲线
  - S3-4：ambush 海盗 HP ×1.1 未实现
  - S3-5：derelict wreck 数量 level 2/4 偏 +1
  - S3-6：ambush 完成后 NPC 残留
  - S3-7：pickEncounterSpawnPos 缺 minDist 避让
  - S3-8：encounterCooldownThisGalaxy Set 字段功能冗余
  - S3-9：trader 资源不足无显式 toast 反馈
  - S3-10：节点 B/C 自检文档化缺失

## §8. 给 PM 节点 D 的重点关注项

15 项必测场景：

1. trader 完整流程（spawn → 飞近 80px → 按 T/Y 交易 → 完成 alert → 离场）
2. distress 援助流程（spawn → 清海盗 → 救援成功 + 奖励 + NPC 飞走）
3. distress 失败流程（忽略 → NPC 死亡 → 救援失败 + 无奖励）
4. ambush 触发流程（飞近 300px → 显形 → 清海盗 → 击退 + 奖励 + 3 enemy-wreck）
5. ambush 远离判定（飞近 350-400px → 飞远到 700+ → 验证是否 expired）
6. derelict 拾荒（4-6 wreck dock 全部）
7. signal 远征（800-1400px 外 dock 单 fragment 6-9 cell）
8. 多事件并发 HUD priority 抢占（ambush 80 > distress 70 > trader 50 > derelict 40 > signal 30）
9. HUD priority 抢占 v0.6.0（assault 100 > 任何 encounter）
10. v0.4.0 escort 完整流程不回潮
11. v0.6.0 hostile-station BOSS 战完全等价
12. v0.7.0 锁敌齐射在 ambush 战斗中正常使用
13. 同关 escort + distress 两组 NPC HUD 独立显示
14. salvage 任务进度不被 derelict 计数干扰
15. 旧存档兼容（清空 localStorage 后 ensureRunRuntimeState 补齐字段）

调试钩子：
- `__gameTest.getEncounters()` 查看 encounter 状态
- `__gameTest.forceActivateEncounters()` 强制激活
- `__gameTest.advanceEncounters(seconds)` 推进 tick
- `__gameTest.tradeEncounter(encounterId, optionIndex)` 模拟 trader 交易

## §9. 给制作人的最终决定建议

**核心结论**：
- v0.8.0 局内随机事件系统在工程实现层面是稳健的（5 种事件完整 + 注册表平行 + RNG 独立 + HUD priority + NPC role 分流 + 不回潮 100%）
- 数值层面 7 项偏离均为 S2/S3 级别，无 S0/S1 阻塞
- 建议进入 PM 节点 D 完整回归 + 主观体验测试

**推荐：选项 A 接受当前实现，进入 PM 节点 D 浏览器回归**
- 3 个 commit ~1300 行新增、5 种事件完整落地、不回潮、单局记忆点机制就位
- 风险：S2-1（trader 15s 时间窗）、S2-3（ambush 远离即 expired）可能让玩家挫败感
- 建议至少修 S2-1（trader dest 距离改为 ≥ 2400px / cap NPC speed 让 30s 内不到达），其他 S2/S3 留 v0.8.1
