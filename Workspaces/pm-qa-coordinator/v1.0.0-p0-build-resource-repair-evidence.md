# v1.0.0 #6 首局 P0 建造 / 采矿 / 资源 / 维修闭环验收

## 基本信息

- 版本名：`v1.0.0-release-candidate`
- 任务：PM 看板 #6 首局 P0 路径建造 / 采矿 / 资源 / 维修闭环
- 验收时间：2026-06-10 03:22-03:31 UTC+8
- 页面：`Game/index.html?playtest=1`
- 浏览器：可见 Chrome + CDP，临时 profile
- 交互方式：CDP `Input.dispatchMouseEvent` 发送可见鼠标点击；playtest hook 仅只读采样
- 证据 JSON：`Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-evidence.json`
- 禁止项确认：未调用 `buildAt`，未直接修改 `state` / resources / storage / body amount，未修改 `Game/`

## 已读取依据

- `Workspaces/pm-qa-coordinator/v1.0.0-release-candidate-kanban.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-mining-coverage-default-layer-retest.md`
- `Workspaces/game-producer-lead-designer/v1.0.0-release-candidate-direction.md`
- `Game/game.js` playtest 只读 hook：
  - `window.__gameTest.playtest.snapshot()`
  - `getBuildPaletteDiagnostics()`
  - `getGridScreenPoint()`
  - `getGridInteractionDiagnostics()`
  - `getMiningCoverageDiagnostics()`
  - `captureMiningExpansionGhostPath()`
  - `getRepairStatusSummary()`
  - `getRuntimeErrors()`
  - `releaseCandidate.getSnapshot()`

## 验收目标

本轮需要从空 / 临时档走首局 P0 建设链：

1. 入口 HUD 可读。
2. 真实 UI 输入建造 `frame` / `power` / `mining` / `turret` / `repair`。
3. 取得采矿收益。
4. 触发资源不足提示。
5. 建成维修站并看到维修反馈。
6. 记录每步输入、`gridKey`、`facility`、resources、toast / statusAlerts、runtime errors、storage signature。

## 实测过程摘要

### Attempt 1 / 2

- Profile：`agentgame-v100-p0-profile`、`agentgame-v100-p0-profile-2`
- 结果：无效验收样本。
- 原因：脚本执行前页面已自然推进到核心摧毁 / 结算态，入口 HUD 已不是首局起点；未能作为 #6 通过证据。

### Attempt 3

- Profile：`agentgame-v100-p0-profile-3`
- 执行方式：启动临时 Chrome 后立即连接 CDP，关闭 PM 面板，只读清空 runtime error buffer，然后按 ghost path 真实点击。
- 初始采样即显示：
  - `goal=查看本局结算`
  - toast 历史为「核心被摧毁，本局结束...」
  - `storageSignature.save.hasValue=true`
  - repair summary：`reasonKey=repair_missing`
  - damage summary：核心 `hp=0`
- 继续点击后只记录到框架扩展与采矿相关状态，未能进入有效首局 P0 链。

## 关键证据

| 项目 | 结果 |
| --- | --- |
| 入口 HUD | 未通过：进入时已是「查看本局结算」 |
| 采矿收益 | 未通过：最终 `harvestingCount=0` |
| power / turret / repair | 未通过：最终未建成 `power` / `turret` / `repair` |
| 资源不足提示 | 未覆盖：未推进到有效资源不足步骤 |
| 维修反馈 | 未覆盖：维修站未建成 |
| runtime errors | 未发现阻断 runtime error，`runtimeErrorCount=0` |
| storage signature | 非空：`save.hasValue=true`，`hash32=0x14a19d77` |
| 最终状态 | `result=fail` |

## PM 结论

#6 不通过，标记为阻塞。

本轮没有取得“从空 / 临时档进入首局，并完成建造 -> 采矿收益 -> 资源不足反馈 -> 维修站建成”的有效证据。相反，`playtest=1` 验收入口在临时 profile 中出现结算态 / 核心摧毁态，导致首局 P0 链无法作为玩家路径通过证据。

#7 自然交火 / 防御 / 维修 / 战后复盘不得继续解锁，需等待 #6 关闭。

## 残留风险与下一步

- 风险：`playtest=1` 自动 PM 检查或页面自然推进可能污染首局入口状态，需要主程确认 playtest 入口是否应默认暂停、是否应避免自动检查影响当前 run。
- 风险：即使采矿收益 #5 已通过，#6 仍可能被自然敌袭、结算态、维修链资源等待或入口状态污染阻断。
- 下一步建议：由主程 / 系统工程检查 `playtest=1` 入口与 PM 自动检查是否会改变当前 run；修复后 PM 用同一脚本重跑 #6。
