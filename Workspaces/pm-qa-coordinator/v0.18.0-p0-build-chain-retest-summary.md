# v0.18.0-p0-build-chain-retest PM 汇总与发布闸门建议

## 基本信息

- 版本名：`v0.18.0-p0-build-chain-retest`。
- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 +08:00。
- 阶段定位：内部 P0 建造链复测阶段，不是发布阶段。
- 输入材料：
  - `Workspaces/game-producer-lead-designer/v0.18.0-p0-build-chain-retest-direction.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-kanban.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-evidence.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-coordinate-diagnostics.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-after-diagnostics.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-audit.md`

## 本轮完成

- 方向：制作人明确 v0.18.0 只做 P0 建造链正确操作复测，不发布、不打 tag、不归档 `Releases/`。
- 看板：PM 已建立当前 v0.18 看板，写明范围、风险、任务、验收标准、证据点和发布默认策略。
- 复测方案：已定义“先扩 `frame`，再点击已有空 `frame` 替换为设施”的记录口径，并区分真实鼠标、真实触控、可见浏览器 CDP、只读 hook 和状态构造。
- 可见 Chrome/CDP 初测：已用临时 profile 进入 `Game/index.html?playtest=1`，完成可见浏览器级取证；结果显示 runtime errors 为 0，但目标 `gridKey` 与实际命中格不稳定，完整链路未关闭。
- 只读坐标诊断 helper：已新增并说明 `Renderer.worldToScreen` 与 `getGridScreenPoint`、`getGridInteractionDiagnostics`、`getPointerGridDiagnostics`、`compareGridClickAlignment`，用于校准和断言坐标，不写 state/storage，不调用 `buildAt`。
- 诊断后复测：已回到原可见 Chrome/CDP 路径，使用 helper 校准点击并记录 `aligned=true`、`screenDelta.distance=0` 的浏览器级证据。
- 专项审核：已完成技术边界、不回潮回归和 PM 证据口径审核，结论为 helper 技术边界通过、不回潮通过，但发布闸门仍关闭。

## 关键通过

- 新增 helper 技术边界通过：helper 只读诊断，不修改游戏状态、资源、存档、storage、建造规则、数值、战斗或 Meta。
- `power` / `mining` / `turret` 的 `frame` 替换链路已通过坐标诊断关闭原失败路径：诊断后复测中目标格与命中格对齐，设施均按“先扩 `frame`，再替换同一 `frame`”完成。
- Runtime errors、console errors、unhandled rejections 均为 0。
- 不回潮回归通过：未修改建造合法性、资源成本、设施数值、RNG、存档、`SAVE_KEY`、meta schema、战斗、敌人、星系或 Meta 规则。

## 未通过 / 未覆盖

- `repair` 未通过：目标 `0,-2` 已扩出 `frame`，但替换为维修站时资源不足，记录为 metal need 44 / have 23 / gap 21，且需要 plasma 6。
- 采矿收益未形成：`mining x1` 建成，但未覆盖资源外环，等待后 `harvestingCount=0`，资源变化不能归因于采矿。
- 真实人工鼠标未覆盖，真实触控未覆盖；可见 Chrome + CDP + PointerEvent 只能算浏览器级/辅助输入证据，不能包装成人类体验通过。
- 自然交火、战后复盘和 Meta 回流仍阻塞；当前证据不足以解锁发布准备。

## PM 发布闸门建议

PM 建议接受 v0.18.0 为“建造链关键替换路径部分闭环 + 坐标取证能力建立”的内部阶段成果。

本阶段不建议进入发布准备。`power` / `mining` / `turret` 的替换链路已经能关闭原“坐标命中不一致”失败路径，但 `repair` 建成、采矿收益和真实人工输入仍缺证，不能证明首局完整建设能力，也不能支撑自然交火、复盘和 Meta 回流。

下一阶段应优先处理资源 / 采矿链与 `repair` 建成证据：先确认资源采集或资源规划如何支持维修站建成，再补 `repair` 完整替换证据；采矿链应证明矿站覆盖资源外环并形成可归因收益。以上闭环后，再判断是否解锁自然交火、战后复盘和 Meta 回流补证。

## 不回潮与发布边界

- 未改规则：不修改建造合法性、设施放置条件、战斗判定或 AI。
- 未改数值：不修改资源成本、设施数值、采矿范围、电力、维修成本或资源生成公式。
- 未改存档 / Meta schema：不修改 `SAVE_KEY`、storage 结构、meta schema 或局外成长结构。
- 不发布：不启动发布准备，不打 tag，不创建或归档 `Releases/`。
- 不提交 dirty `42.md`：本阶段提交范围只应包含本 summary 与当前 v0.18 看板更新。

## 验收结论

- 自动检查：helper 技术边界与不回潮回归通过；提交前仍需执行 `git diff --check`。
- 浏览器级检查：可见 Chrome/CDP 初测与诊断后复测已覆盖，runtime errors 为 0。
- 人工体验：真实鼠标和触控未覆盖。
- 未覆盖路径：`repair` 建成、采矿收益、自然交火、战后复盘、Meta 回流。
- 是否阻塞发布：阻塞。v0.18.0 只能作为内部阶段接受，不进入发布准备。
