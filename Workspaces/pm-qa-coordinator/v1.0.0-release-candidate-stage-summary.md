# v1.0.0 发布闸门阶段汇总

## 基本信息

- 版本名：`v1.0.0-release-candidate`
- 汇总角色：`/pm-qa-coordinator`
- 汇总时间：2026-06-10
- 阶段结论：不进入发布准备；不打 tag；不归档 `Releases/`。
- 严格范围：本汇总只记录当前阶段 PM 发布闸门状态，不替代制作人最终批准。

## 读取依据

- `Workspaces/pm-qa-coordinator/v1.0.0-release-candidate-kanban.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-mining-coverage-default-layer-retest.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-entry-contamination-triage.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-build-plan-fix.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-retest.md`
- `Workspaces/game-producer-lead-designer/v1.0.0-release-candidate-direction.md`

## 已完成事项

- 制作人方向已明确：`v1.0.0-release-candidate` 是正式大版本交付候选方向，不等于立即发布；发布、tag、`Releases/` 归档仍需完整 gate 和制作人最终批准。
- PM 总看板已建立：当前默认结论为不发布、不打 tag、不归档 `Releases/`，并拆出 #1-#14 发布闸门任务。
- 采矿覆盖产品方案已完成阶段决策：制作人批准默认层 `A + E + F`，即金属缺口诊断、候选路径或 ghost path、移动 + 重建教学；未批准采矿范围 / 公式、资源布局、成本、移动物理、建造合法性、存档或 Meta schema 变更。
- 默认层实现已完成并进入复测：看板记录 `affordability`、`ghostPath`、`mobileApproachHint`、resourceGuide 与 PM 只读 hook 已暴露，基础自检通过。
- 采矿收益单项 gate 已通过：宽窗口真实鼠标/CDP 路径下建成采矿站，取得 `harvestingCount=1`、目标资源下降与玩家资源增加证据。
- playtest 入口污染已修复并复测关闭：`playtest=1` 入口默认暂停，不再自动执行 release candidate checks；全新临时 profile 首帧非结算态、storage 为空。
- P0 建造计划修复已完成阶段实现：`getMiningBuildPlan().currentStep` 可输出逐步 `frame -> mining` 计划，并在 04:12 复测中推进到 `mining@5,0` 建成。

## 关键通过证据

- 采矿单项可取得 `harvestingCount=1` 与收益：`小行星-3 / metal` 样本中 `body.amount` 从 `332.533` 降至 `280.311`，玩家 `metal` 从 `92.009` 增至 `151.059`。
- 入口污染关闭：全新临时 profile 打开 `Game/index.html?playtest=1` 时首帧 `paused=true`，按钮为「继续」，未进入本局结算，`objective.failed=false`。
- runtime errors 为 0：#6 复测初始与最终采样均无阻断 runtime error。
- storage 空档可用：全新临时 profile 下 `save.hasValue=false`，`corruptRaw.hasValue=false`，可作为空档首局入口证据。

## 未通过与阻塞

- 完整 #6 首局 P0 建设链仍阻塞：虽然入口污染已关闭，且 `getMiningBuildPlan().currentStep` 可推进到采矿站建成，但最终未形成建造、采矿、资源规划、维修的可交付闭环。
- 采矿稳定性未通过：04:12 复测过程中曾出现 `harvestingCount=1`，但最终 `harvestingCountFinal=0`，矿站回到 out-of-range，提示仍为「采矿站待命」/「采矿站未覆盖资源」。
- `repair` 未建成：最终 `repairBuilt=false`，repair summary 为 `repair_missing`，`repairerCount=0`，存在受损对象但维修链无有效反馈。
- 真实人工与触控未覆盖：现有证据以可见 Chrome、CDP 鼠标事件和只读 hook 为主，不能替代人工桌面鼠标体验、小屏触控或等价触控模式。
- #7/#8/#9/#10/#11/#14 均不能发布通过：#7 仍被 #6 阻塞；#8 Meta / 星系推进未验收；#9 真实输入 / 触控 / 小屏未覆盖；#10 release candidate 自动检查与浏览器 JSON 未形成完整结论；#11 发布包 / Release 归档方案未执行；#14 制作人最终批准未发生。

## Gate 状态

- 玩家路径 Gate：未通过。采矿单项收益成立，但完整 P0 建设链、维修、防御、复盘、星系推进、Meta 回流未闭环。
- 质量 Gate：未通过。runtime errors 当前为 0，但真实人工输入、小屏触控、HUD 遮挡与维修失败路径未覆盖。
- 技术 Gate：阶段性部分通过。playtest 入口污染已关闭，storage 空档可用；但 release candidate checks、自动检查覆盖范围、调试边界和完整浏览器 JSON 仍未完成发布级汇总。
- 发布 Gate：未通过。没有制作人最终批准；不允许 tag、构建发布包或归档 `Releases/`。

## 问题分级

- S0：0。当前未发现会要求立即停止仓库工作的灾难级问题。
- S1：发布阻塞。完整 P0 未通过，#6 首局建设链未闭环，直接阻止 #7 及后续发布 gate。
- S2：采矿覆盖稳定性、repair 链、真实输入 / 触控。采矿站可短暂收益但不稳定，维修路径未建成，真实人工和触控路径未覆盖。
- S3：提示 / 证据口径。resourceGuide 旁证、JSON 采样字段、HUD 遮挡和证据表述仍需统一，避免把辅助 hook 包装成人工通过。

## PM 建议

PM 建议当前阶段不进入发布准备，不打 `v1.0.0-release-candidate` tag，不创建或归档 `Releases/`。下一阶段必须优先处理「采矿覆盖稳定性 / 工站保持范围 / repair 路径」这一核心体验阻塞；如果仅靠默认层引导无法稳定关闭 #6，则可能需要回到高风险产品流程，由制作人重新决策采矿范围 / 公式、资源布局、移动能力、建造合法性或数值方案。

## 残留风险

- 采矿收益单项已经成立，但不能外推为首局 P0 主路径通过。
- 04:12 复测使用 resourceGuide 的剩余量变化作为旁证，底层 JSON 的 `targetAmounts.amount` 字段为 `null`，证据口径仍需后续修正。
- 如果继续推进 #7/#8/#10/#11，会掩盖 #6 基础体验阻塞，产生伪发布风险。
- 当前汇总不包含 tag、构建、发布包归档或制作人最终批准。
