# v0.15.0 P0 人工路径体验记录

## 证据 P0-20260609-CDP-01

- 任务关联：#5 T2 P0 人工路径体验记录。
- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-09 22:43 +08:00。
- 环境：Windows 10.0.26200；可见 Chrome 148；CDP 端口 `9318`。
- URL：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 视口：实际 `1267x627`，DPR `2`；Chrome `--window-size=1280,720`；非移动设备模拟，非真实触控。
- 输入方式：可见 Chrome + CDP；DOM click、键盘 Enter、只读 DOM/JSON 摘要；建造放置用运行态 `mouseWorld` + Enter 避免合成 PointerEvent 的 `setPointerCapture` 异常。
- 存档状态：隔离临时 profile `C:\Users\ylswd\AppData\Local\Temp\agentgame-v015-manual-p0-profile-20260609-2250-9318`；起始 `localStorageLength=0`、`sessionStorageLength=0`、`hasGameSave=false`。未删除或清空用户真实存档。
- 原始 JSON：`Workspaces/pm-qa-coordinator/v0.15.0-browser-evidence-manual-p0-cdp-20260609-2250.json`。
- 参考 #4 JSON：`Workspaces/pm-qa-coordinator/v0.15.0-browser-evidence-gate-rc-20260609-223408.json`；摘要 `Workspaces/pm-qa-coordinator/v0.15.0-browser-evidence-browser-json.md`。
- 取证失败记录：第一次脚本尝试在浏览器中合成 `PointerEvent`，触发 `setPointerCapture` 的 `NotFoundError`，该次不计为路径通过证据；第二次改用键盘 Enter + 运行态光标定位，CDP 事件无异常。

## 运行态摘要

- Runtime errors：最终 `0`；每个路径节点记录的 `runtimeErrors` 均为空。
- Release candidate checks：第二次路径后 `ok=true`；automatic passed `12`；failed `0`；warning `runtimeState.signatureStable`。
- FrameTiming：rAF `30` 帧；avg `16.43999999920537ms`；max `16.80000000000291ms`；longFrameCount `0`。
- Storage signature：checks 前后 unchanged；路径过程中通过 Meta 点数写入形成临时 profile 内 `localStorage`，未触碰用户真实 profile。
- Hidden checklist：`releaseCandidatePanel` 可打开；10 个人工项默认空值，工具不会自动标记人工通过。

## P0 路径记录

| P0 项 | 状态 | 操作步骤摘要 | 实际结果 | 证据与发布影响 |
| --- | --- | --- | --- | --- |
| 1. 空存档首局 / 隔离存档进入游戏 | 通过（浏览器/CDP） | 临时 profile 打开 `?playtest=1`，确认无本地存档后用 `resetRun(1505001, 0, "emptyVoid")` 隔离首局。 | 首屏 HUD 可读：`已完成 0 项任务 / 1 人缩放`、`第 1 关 / 共 7 关`、资源 `金属130/矿物60/气体35/等离子8`；引导提示先建发电站/采矿站；runtime errors 0。 | 可支撑“隔离空档入口可打开”。不等同肉眼手感评分。 |
| 2. 建造/采矿链 | 通过（浏览器/CDP，非完整手玩） | 点击 `采矿站`，用 Enter 放置在周边框架；随后观察资源和采矿提示。 | 采矿站从框架替换建成，toast `采矿站已建造`；后续资源从 `金属18/矿物20` 自然增长到 `金属36/矿物23`；资源引导显示采矿站就绪/待命、需进入彩色外环。 | 证明建造入口、采矿设施与资源变化可观察；未覆盖鼠标真实点击格子的手感。 |
| 3. 资源不足 / 断电反馈 | 通过（浏览器/CDP 设置场景） | 将当前资源临时置零后点击 `炮塔` 并尝试放置；观察采矿站过量导致的电力提示。 | 建造卡显示 `缺金属35`，状态栏显示 `建造「炮塔」资源不足：金属35 矿12`；采矿站场景显示 `采矿站因电力不足未运作`、`电力紧张：6/6`、`断电2`。 | 资源不足和电力不足文案可读；但“断电”来自 CDP 构造的过量采矿站/资源置零场景，仍需真人路径复核。 |
| 4. 敌人接近 / 战斗威胁 | 部分覆盖（CDP/test hook 辅助） | 自然等待约 32 秒未观察到敌人；随后通过完成任务并打开星系跳转路径时继续观察。 | 自然等待阶段 `enemyCount=0`；星系跳转前后出现 `威胁1`、`最近 海盗 1097m → 右侧`、`敌袭临近：无火力或无防御`，战斗摘要显示 `1 个敌对目标`。 | 证明威胁提示字段可读，但自然首局等待未稳定触发敌人，不算完整人工通过。 |
| 5. 武器 / 护盾 / 维修反馈 | 未覆盖（仅观察到缺失原因） | 在无武器/无护盾/无维修设施的威胁状态下读取战斗摘要。 | 摘要显示 `火力：当前无炮塔或导弹井`、`护盾：当前无护盾设施`、`维修：当前无可修对象`。未发生真实交火、受击、护盾吸收或维修动作。 | 阻塞 #9 进入发布准备：实际武器开火、护盾、维修反馈仍需补证。 |
| 6. 战斗后复盘 | 未覆盖 | 威胁窗口后读取 `combatReviewSummary`。 | `combatReview` 为空，未形成交火后复盘。 | 阻塞 #9 进入发布准备：需要真实或可复查交火后复盘证据。 |
| 7. 星系跳转 | 通过（CDP/test hook 辅助） | 用 `completeCurrentObjective()` 完成当前任务，点击 `跃迁下一星系`，再点击星系卡。 | 星系面板可见，卡片显示 `稳定贸易区`/`海盗领地`、风险和资源倍率；点击卡片后 `level=1`，`currentGalaxyType=tradeHub`，HUD 变为 `第 2 关 / 共 7 关`。 | 跳转 UI 和状态变化可复查；自然完成任务过程由 test hook 辅助，不能代表完整人工流程通过。 |
| 8. Meta 回到新局 | 未覆盖（仅 CDP 等价辅助） | 构造结算态并读取结算/Meta 文本；为避免 eval 中 reload 破坏证据，未点击 `开始新局`，改用 `resetRun` 做非破坏等价回新局。 | 结算文本可读，包含局外点数、可购买建议和“开始新局”按钮文案；`resetRun` 后回到第 1 关，Meta 点保留为临时 profile 内 2 点。实际 UI restart 点击未覆盖。 | 阻塞 #9 进入发布准备：Meta 面板操作、结算按钮点击、真实回到新局仍需补证。 |

## 证据分层结论

- 自动检查：通过。#4 与本次路径后的 release candidate checks 均无 blocking fail；runtime errors 为 0。
- 真实浏览器/CDP：部分覆盖。空档入口、建造/采矿、资源不足/断电、威胁提示和星系跳转有可复查 DOM/JSON 摘要。
- 人工体验：未完整覆盖。本次没有真人肉眼/手感确认，且多个节点依赖 CDP/test hook 辅助。
- 未覆盖路径：真实鼠标放置手感、自然敌人接近到交火、武器开火、护盾受击、维修动作、战斗后复盘、结算面板点击开始新局、真实触控。
- 是否阻塞发布：阻塞进入发布准备。#5 已完成一次 P0 路径尝试和记录，但不能判定 P0 人工路径完整通过；#9 当前不得建议进入发布准备交接。

## 后续动作

- #6 继续补三视口/小屏/复制摘要/触控假设证据。
- #8 专项审核时重点复核：本次“浏览器/CDP 证据”和“未覆盖人工体验”是否被正确分层。
- 若要解除 #5 残留风险，需要可见浏览器真人或更接近真人的 CDP 输入复测：自然完成一轮交火，记录武器/护盾/维修和战斗后复盘，再点击结算“开始新局”完成 Meta 回流。
