# v0.15.0 Browser JSON Evidence

## 证据 BE-20260609-CDP-01

- 任务关联：#4 T1 真实浏览器运行态 JSON 取证尝试。
- 记录人：`/senior-systems-engineer` + `/pm-qa-coordinator`。
- 时间：2026-06-09 22:34:08 +08:00。
- 操作系统：Windows 10.0.26200。
- 浏览器：Chrome 148.0.7778.217。
- 是否真实浏览器：是。使用普通可见 Chrome 窗口，CDP 只负责在页面上下文中执行等价 console 调用。
- 是否 headless：否。未使用 `--headless`；UA Headless 探测为 `false`，`navigator.webdriver` 为 `false`，`environment.isHeadlessHint` 为 `false`。
- URL：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 视口：启动请求 `1280x720`；JSON 实际 `1267x627`，DPR `2`；设置方式为 Chrome `--window-size`，不是 DevTools 设备模拟，也不是真实移动设备。
- 起始条件：隔离临时 profile `C:\Users\ylswd\AppData\Local\Temp\agentgame-v015-cdp-profile-20260609-223408-9305`；原始 JSON 显示 `hasSave=false`。
- 启动命令：`C:\Program Files\Google\Chrome\Application\chrome.exe --remote-debugging-port=9305 --user-data-dir=C:\Users\ylswd\AppData\Local\Temp\agentgame-v015-cdp-profile-20260609-223408-9305 --no-first-run --disable-default-apps --window-size=1280,720 --new-window file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 调用函数：`window.__gameTest.playtest.runReleaseCandidateChecks({ sampleFrames: 30 })`。
- 原始 JSON：`Workspaces/pm-qa-coordinator/v0.15.0-browser-evidence-gate-rc-20260609-223408.json`。
- 机器摘要：`Workspaces/pm-qa-coordinator/v0.15.0-browser-evidence-gate-rc-20260609-223408-summary.json`。

## JSON 摘要

- `ok`：`true`。
- checks：total `13`；blocking fail `0`；failed `0`；warn/unknown `1`。
- blocking checks：无。
- failed checks：无。
- warning checks：`runtimeState.signatureStable`。
- storageSignature before save/corruptRaw：`null` / `null`。
- storageSignature after save/corruptRaw：`null` / `null`。
- storageSignature 是否一致：`true`；`storage.unchanged` 为 pass。
- runtimeStateSignature 是否完全一致：`false`。主要差异是 rAF 采样期间 `metal` 从 `131` 自然推进到 `132`；checks 将其标为 warn，非 blocking。
- frameTiming：mode `rAF`，sampleCount `30`，avgMs `16.14999999960264`，maxMs `16.800000000000182`，longFrameCount `0`，threshold `33`。
- runtimeErrors：自动 buffer count `0`；CDP 调用期间 `console.error` 捕获 `0` 条。
- keyDom：`keyDom.present` 为 pass；原始 JSON 中 `snapshotAfter.base.keyDom` 与 `snapshotAfter.hud.keyDom` 可复查。
- hookStatus：design/combat/build/meta/galaxy hooks 均 available；原始 JSON `snapshotAfter.hookStatus` 可复查。
- evidence layers：automaticPassed `12`，automaticFailed `0`，automaticUnknown `0`，browserCovered `2`，browserUncovered `2`，manualCovered `0`，manualUncovered `8`。
- residualRisks：`runtime_state_drift` warn；`browser_uncovered` info；`manual_uncovered` info。

## 证据类型判定

- 自动检查：已在真实浏览器页面运行。
- 浏览器级检查：已取得真实浏览器 JSON，非 headless，非 Node/VM。
- 人工体验：未覆盖；本证据只覆盖 #4，不覆盖 #5 P0 人工路径。
- 三视口：未覆盖；本次只使用一个普通窗口视口，不覆盖 #6。
- 真实触控：未覆盖。

## 清理与失败记录

- CDP 等待：15 秒内成功访问 `http://127.0.0.1:9305/json/list`。
- 临时失败：前两次 PowerShell 临时脚本包装在解析阶段失败，均发生在浏览器启动前，未形成 browser 证据。
- 清理策略：脚本结束时调用 `Browser.close`，必要时 `Stop-Process -Id <pid>`，并尝试删除自建临时 profile。
- 本次失败命令：无；修正为纯 ASCII 临时脚本后成功取得 JSON。
- 不能算 browser 证据的部分：脚本包装失败不算 browser 证据；CDP 自动调用不等同人工体验，不能证明手感、P0 路径、三视口操作或真实触控。

## PM 结论

`#4` 的真实浏览器运行态 JSON 最低证据已取得，可进入 #5/#6 补充人工路径、三视口、复制摘要和触控假设记录。由于 P0 人工路径和三视口仍未覆盖，#9 当前仍不能建议进入发布准备，只能在后续证据齐全后再判断。
