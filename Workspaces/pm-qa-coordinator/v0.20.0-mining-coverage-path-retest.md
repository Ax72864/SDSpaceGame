# v0.20.0 #6 原路径复测：采矿覆盖与收益归因

## 基本信息

- 记录人：`/pm-qa-coordinator`
- 时间：2026-06-10 02:13-02:19 UTC+8
- 入口：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`
- 浏览器：可见 Chrome + CDP
- 临时 profile：
  - `C:\Users\ylswd\AppData\Local\Temp\agentgame-v020-retest-clean-20260610-021531-9941`
  - `C:\Users\ylswd\AppData\Local\Temp\agentgame-v020-retest-paused-20260610-021649-9942`
  - `C:\Users\ylswd\AppData\Local\Temp\agentgame-v020-retest-finalattempt-20260610-021842-9943`
- 原始摘要 JSON：`Workspaces/pm-qa-coordinator/v0.20.0-mining-coverage-path-retest-20260610-summary.json`
- 边界：未修改 `Game/`，未调用 `buildAt`，未直接改 `state` 伪造通过，未清空或覆盖用户真实存档；临时 profile 中 `localStorage/sessionStorage` 均为空。

## 读取依据

- 已读取 `Workspaces/pm-qa-coordinator/v0.20.0-mining-coverage-path-evidence-kanban.md`。
- 已读取 `Workspaces/pm-qa-coordinator/v0.20.0-mining-coverage-diagnostics.md`。
- 已读取 `Workspaces/pm-qa-coordinator/v0.20.0-mining-coverage-path-evidence.md`。
- 已读取 `Game/game.js` 中 `computeMiningCoverageDiagnostics()` 与 `__gameTest.playtest.getMiningCoverageDiagnostics(options)` 出口。

## 主复测流程

主证据采用端口 `9942` 的暂停输入轮次：加载后立即暂停，避免相机 / 工站移动导致点击漂移；通过 DOM 选择建造按钮，通过 CDP 指针点击画布坐标，铺路完成后恢复运行等待 12 秒。

初始只读诊断：

- 最近资源：`恒星-1/plasma`，`distance=610.67`，`range=279.68`，`gap=331`，`body.amount=9999`。
- `bestCandidate=-3,-12`，对 `恒星-1/plasma` 为 `distance=265.13`，`range=279.68`，`gap=0`。
- `suggestedExpansionPath` 为 14 步 frame：`-2,0 -> -3,0 -> -3,-1 -> -3,-2 -> -3,-3 -> -3,-4 -> -3,-5 -> -3,-6 -> -3,-7 -> -3,-8 -> -3,-9 -> -3,-10 -> -3,-11 -> -3,-12`，`finalAction=expand_frame_then_mining`。

步骤记录：

| 步骤 | target gridKey | facility | resource / target | 结果 | toast / statusAlerts / runtime |
| --- | --- | --- | --- | --- | --- |
| 1 | `1,0` | `power` | 无 | 成功建成；`aligned=true`；资源从 `metal=130, ore=60` 到 `metal=108, ore=52` | runtime errors `0` |
| 2 | `-1,1` | `mining` | 最近 `恒星-1/plasma`，rangeHint `gap=345` | 未建成；坐标诊断 `aligned=true` 且 placement `valid/affordable`，但该屏幕点落在 HUD 覆盖区域，未触发 canvas 建造 | 未作为规则失败；记录为原路径输入落点失败 |
| 3 | `-2,0` 到 `-3,-11` | `frame` | 通往 `-3,-12` 候选 | 逐格建成；资源降到 `metal=4, ore=52` | runtime errors `0` |
| 4 | `-3,-12` | `frame` | `恒星-1/plasma` 入圈候选 | 未建成；剩余 `metal=4`，frame 需要 `metal=8`，缺 `metal=4` | toast/statusAlerts 指向金属不足，建造模式保持 |

补充轮次 `9943` 对候选格执行过 `mining` 选择和点击尝试，但该轮从第一步开始指针递送不稳定，候选 frame / path 前置不存在，因此只作为“候选 mining 已尝试但未证明成功”的辅助证据，不纳入主流程通过判断。

## 等待窗口与收益归因

等待前（暂停构建结束）：

- `resources={ metal:4, ore:52, gas:35, plasma:8, research:0, powerUsed:0, powerAvailable:6 }`
- `minerCount=0`，`activeCount=0`，`harvestingCount=0`，`target=null`
- `恒星-1/plasma body.amount=9999`

恢复运行等待 12 秒后：

- `resources={ metal:14, ore:54, gas:36, plasma:8, research:0, powerUsed:0, powerAvailable:16 }`
- `minerCount=0`，`activeCount=0`，`harvestingCount=0`，`target=null`
- `恒星-1/plasma body.amount=9999`
- 归因：资源增长来自自然 / 核心收入；没有矿站存在，目标资源 `body.amount` 未下降，不能归因采矿收益。

## 辅助轮次

- `9936`：页面在采样前已进入结算面板，判定为状态污染，丢弃，不计通过 / 失败。
- `9941`：未暂停时相机 / 工站移动导致点击漂移，`power@1,0` 指针实际映射到 `5,-5`，记录为输入映射失败。
- `9943`：补充候选 mining 最终尝试，但指针递送不稳定，保留为“输入证据不足”辅助，不作为通过证据。

## PM 结论

- #6 未通过：未关闭 v0.19 原路径 `mining=-1,1` 未覆盖资源外环 / 收益未形成的问题。
- 当前不能证明规则不可达，也不能证明规则成功；本轮阻塞由两部分组成：
  - 原 `mining=-1,1` 可见点击受 HUD 覆盖 / 输入递送影响，未能稳定建成。
  - helper 建议路径理论入圈，但从当前资源出发，真实 frame 链在候选前被 `metal` 成本阻断。
- 不属于电力失败：恢复运行后 `powerAvailable=16`，`powerUsed=0`，不存在矿站耗电。
- 不属于 runtime 失败：runtime errors、console runtime errors、unhandled rejections 均为 `0`。
- 不属于收益归因成功：`harvestingCount=0`，`target=null`，目标资源 `body.amount` 未下降。

## #7 判断

自然交火、战后复盘和 Meta 回流继续阻塞。当前没有可归因采矿收益证据，也没有可接受的“原路径不可达但反馈闭环”证据；#7 不应解锁，只能等待下一轮先解决原路径输入可靠性或重新规划可见操作路径。

## 残留风险

- 未覆盖真实人工鼠标和真实触控。
- CDP 画布点击在 HUD 覆盖区域仍不能等价真实玩家成功输入。
- helper 的 `suggestedExpansionPath` 不考虑成本；本轮证明它给出的入圈路径可能真实可达性受资源成本阻塞。
- 若后续要关闭 #6，建议先确定一种不改 state 的可靠输入路线：更大视口 / 明确避开 HUD 的相机位置 / 人工鼠标复测，或进入最小输入可读性修复评估。
