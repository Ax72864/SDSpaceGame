# v0.18.0-p0-build-chain-retest #3 可见浏览器建造链复测证据

## 基本信息

- 任务：#3 T1 真实/可见浏览器建造链复测。
- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 00:22-00:24 +08:00。
- 环境：Windows 10.0.26200；可见 Chrome 148.0.7778.217；CDP 端口 `9331`、`9332`。
- 页面入口：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 存档策略：使用临时 Chrome profile；未删除、清空或提交用户真实存档。
- 原始 JSON：
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-evidence-20260610-cdp.json`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-evidence-20260610-cdp-enter.json`

## PM 结论

本轮已完成可见 Chrome + CDP 输入尝试，但 #3 未通过，v0.18.0 建造链前置缺口未关闭。

主要原因不是 runtime error，也不是当前证据能证明 `Game` 建造规则错误，而是自动化输入仍无法稳定证明目标 `gridKey` 与实际命中 `gridKey` 一致。第一轮能建成 `power x1` 与 `mining x1`，但声明目标与实际设施落点不一致，且 `mining` 不是在本轮刚扩出的目标 `frame` 上替换成功；`turret` 和 `repair` 均失败。第二轮改用 `mouse move + Enter` 后只建成 `power x1`，后续 `frame` 扩建继续命中已有结构，无法闭环。

因此，本轮不能包装为“正确真实操作通过”。#4 最小 UI/反馈修复暂不触发，因为失败证据仍更像 CDP 坐标/输入映射问题，而非真实玩家按正确步骤失败。

## 分项结果

| 项 | 方式 | 结果 | Runtime errors | 结论 |
| --- | --- | --- | --- | --- |
| 入口 / 首局 / HUD | 可见 Chrome + 临时 profile + CDP | 通过。页面可进入，`window.__gameState` / `window.__gameTest` 可读，起始设施为 `core x1`、`frame x4`。 | 0 | 可作为浏览器级取证入口 |
| `frame -> power` | CDP 数字键选择 + mouse click / Enter | 部分通过。第一轮最终 `power x1` 建成，实际落点为 `0,-2`；第二轮最终 `power x1` 建成，实际落点为 `0,-1`。 | 0 | 证明设施可替换已有 frame，但目标声明与命中仍需校准 |
| `frame -> mining` | CDP 数字键选择 + mouse click | 部分通过。第一轮最终 `mining x1` 建成，资源从约 `102.75/52.58` 变为 `75.15/42.67`，但该次不是在本轮刚扩出的声明目标 `-1,-1` 上闭环，实际设施落到 `0,-1`。 | 0 | 不计完整正确链路通过 |
| `frame -> turret` | CDP 数字键选择 + mouse click / Enter | 失败。第一轮和第二轮均未建成 `turret`，toast/statusAlerts 出现“设施只能建造在已连接的空框架上。” | 0 | 阻塞 |
| `frame -> repair` | CDP 等号键选择 + mouse click / Enter | 失败。未建成 `repair`，toast/statusAlerts 出现“设施只能建造在已连接的空框架上。” | 0 | 阻塞 |
| 采矿链观察 | 第一轮建成 `mining x1` 后等待约 6 秒 | 未通过采矿链。statusAlerts 显示“采矿站待命：距「行星-1」还差 353，请进入彩色外环”“采矿站未覆盖资源”。 | 0 | 采矿收益未闭环 |

## 关键证据摘录

### 第一轮 `cdp.json`

- 环境：可见 Chrome，CDP 端口 `9331`，临时 profile `agentgame-v018-p0-profile-20260610-002221-9331`。
- 输入：CDP keyboard hotkeys + mouse move/click。
- 最终设施计数：`core x1`、`frame x3`、`power x1`、`mining x1`。
- 最终资源：`metal 82.81`、`ore 44.29`、`gas 36.53`、`plasma 8.32`、`research 0`。
- runtime errors：0。
- 阻塞提示：`turret` / `repair` 后均记录“设施只能建造在已连接的空框架上。”
- 证据边界：脚本声明目标多次为 `-1,-1`，但实际建成设施落到 `0,-2` 或 `0,-1`，目标 gridKey 与实际命中不一致。

### 第二轮 `cdp-enter.json`

- 环境：可见 Chrome，CDP 端口 `9332`，临时 profile `agentgame-v018-p0-profile-20260610-002346-9332`。
- 输入：CDP keyboard hotkeys + mouse move 后 Enter。
- 最终设施计数：`core x1`、`frame x3`、`power x1`。
- 最终资源：`metal 120.06`、`ore 54.55`、`gas 36.70`、`plasma 8.35`、`research 0`。
- runtime errors：0。
- 失败点：`mining`、`turret`、`repair` 均未建成；多次 `frame` 扩建提示“该格已有结构，建造模式仍保持”。
- 证据边界：Enter 放置仍没有稳定命中声明目标 `gridKey`，不能证明正确玩家路径。

## 未计入通过的尝试

后续尝试加入“移动后读回实际 gridKey”的校准脚本，但 PowerShell/CDP 取证脚本在表达式转义和对象属性写入处失败，未生成新的有效证据文件。该失败属于取证脚本错误，不计为游戏 runtime error，也不作为游戏规则失败。

## 复测结论

- 自动检查：`Game/game.js` 静态 hook 与规则边界已确认；`git diff --check` 待提交前执行。
- 浏览器级检查：已覆盖可见 Chrome 入口、临时 profile、CDP input、只读状态记录。
- 人工体验：未覆盖真实手持鼠标的人类操作；未覆盖真实触控。
- 建造链：未关闭。只能证明部分设施可建成，不能证明完整正确链路通过。
- 采矿链：未关闭。`mining x1` 未覆盖资源外环，没有形成资源增长证据。
- 是否触发 #4：暂不触发。当前失败仍更支持输入/坐标取证问题，不能证明真实玩家正确点击已有 `frame` 仍失败。
- 是否阻塞后续：继续阻塞自然交火、复盘、Meta 回流和发布准备。

## 下一步建议

- 优先由真实人工鼠标在可见浏览器中按方案手动点击，并由只读 hook 旁路记录实际 `gridKey` / `facility`。
- 若继续用 CDP，先单独完成“屏幕坐标 -> `mouseWorld` -> `gridKey`”校准工具，再进入建造链复测。
- 只有当人工正确点击同一实际新增 `frame` 仍失败时，才进入 #4 最小 UI/反馈/取证断言修复。
