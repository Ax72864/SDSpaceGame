# v0.10.0 局外持久化成长 PM 看板

版本名：`v0.10.0-meta-progression`

制作人方向文档：`Workspaces/game-producer-lead-designer/v0.10.0-meta-progression-direction.md`

制作人方向 commit：`52ae80d`

## 本轮目标

把当前局外点数从“几个加数值按钮”升级为可规划的持久天赋树与开局策略选择。玩家完成一局后，无论失败还是通关，都能知道本局获得了多少局外成长、现在能买什么、下一局适合尝试哪条路线。

v0.10.0 不追求新增大类战斗内容，而是把 v0.1-v0.9 已有的局内建造、任务、研发、战斗和星系路径接到一个清晰的跨局成长闭环上。

## 背景与制作人方向摘要

- 当前代码已有简化 meta：`SAVE_KEY = "star-ring-station-meta-v1"`、`state.meta.points`、`mining / hull / weapons` 和 `unlocks.weaponFrame / efficientCore`。
- 制作人判断现有局外成长方向成立，但体验仍停留在几个按钮，缺少前置关系、节点等级、路线规划、开局协议、结算引导和存档迁移策略。
- v0.10.0 核心是保留玩家已有点数和旧字段价值，把旧 meta 升级为正式 `META_TALENT_TREE` 注册表、`talents` ranks、`selectedStartProtocol` 和集中效果 helper。
- 首批体验目标：失败也有去处、路线选择明确、开局协议可感知、局内研发和局外天赋边界清楚、不污染 v0.9 galaxyMap。
- 制作人明确不做联机、异步对战、新大类建筑、新 enemy kind、新关卡系统、完整剧情、云存档或付费经济系统。

## 风险定级

- 风险等级：项目高风险。
- 原因 1：涉及 `SAVE_KEY`、旧 meta 字段迁移、玩家点数保留和持久化 schema，任何误判都可能导致点数丢失或旧能力失效。
- 原因 2：新增 `META_TALENT_TREE`、效果 helper、开局协议和购买逻辑，会跨采矿、耐久、武器、科研、结算和 UI 多个系统。
- 原因 3：跨局成长数值一旦过强，会压扁 v0.1-v0.9 的首局难度、研发节奏、星系路径风险和战斗压力。
- 原因 4：Meta UI、协议选择和结算提示都要放进现有单页 UI，信息架构不清会造成玩家读不懂、旧按钮重复或局内 / 局外边界混淆。
- 原因 5：v0.9 `galaxyMap` 是局间路径，v0.10 Meta 是局外长期成长，两者若互相改写状态机会污染已完成系统。
- 高风险流程：制作人方向 -> PM 看板 -> ADR / 数值 / UI 方案 -> 分批实现 -> 专项审核 -> PM 回归 -> 制作人最终决定。

## 严格范围边界

### 看板阶段允许修改

- 仅允许新增并修改 `Workspaces/pm-qa-coordinator/v0.10.0-meta-progression-kanban.md`。

### 实现阶段预计允许修改

- 在 #2 主程 ADR、#3 数值方案和 #4 UI / 信息架构方案全部完成并通过 PM 文档门槛后，预计允许实现阶段修改 `Game/game.js`、`Game/index.html`、`Game/style.css` 中与 v0.10.0 Meta 相关的区域。
- 实现阶段的具体文件边界、状态机入口、测试钩子命名和回滚方式必须以 #2 ADR 为准。

### 禁止修改

- 看板创建阶段禁止修改 `Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、其他成员工作区、其他版本看板和方向文档。
- 禁止删除文件，禁止 stash、reset、checkout、clean、force push，禁止改写已推送历史。
- 禁止提交、恢复、覆盖、改写或 stage 当前 dirty 的 `42.md`。
- 未经制作人最终决定，禁止发布、打 tag、归档 `Releases/` 或将 v0.10.0 写入正式 `Docs/`。
- 未完成 ADR / 数值 / UI 方案前，禁止进入 `Game/` 实现。

## 状态总览

### 待办

暂无待办任务。

### 进行中

- #10. 制作人最终决定 / 发布判断（PM #9 自动 + Chrome headless 浏览器级回归通过；待制作人给出体验接受 / 发布判断）

### 待审核

暂无待审核任务。

### 完成

- #1. PM 看板创建（完成时间：2026-05-23；验证：本看板包含本轮目标、背景摘要、风险定级、严格范围、状态总览、10 个任务、QA 回归点和高风险流程；仅允许提交本看板文件）
- #2. 主程技术 ADR（完成时间：2026-05-24；文档：`Workspaces/lead-game-programmer/v0.10.0-meta-progression-adr.md`；commit `d49bb7a`；PM 文档门槛：通过）
- #3. 数值方案（完成时间：2026-05-24；文档：`Workspaces/pm-qa-coordinator/v0.10.0-meta-progression-numerics.md`；commit `4bfb44e`；PM 文档门槛：通过）
- #4. UI / 信息架构方案（完成时间：2026-05-24；文档：`Workspaces/ui-artist/v0.10.0-meta-progression-ui-plan.md`；commit `3d3e858`；PM 文档门槛：通过）
- #5. T1 存档迁移 + META_TALENT_TREE 注册表 + helper（完成时间：2026-05-24；实现 commits `cbf2dbd` + `ec9098f`；S1 修复 commit `c2d20ab`；专项审核：`expert-agent-architect-opus47` 有条件通过，S1-01 已修复；PM 节点 A：通过）
- #6. T2 Meta UI 面板与购买逻辑（完成时间：2026-05-24；实现 commit `3051614`；S1 修复 commit `d9b9cfa`；专项复审：S1 已关闭；PM 节点 B：通过）
- #7. T3 开局协议 + 结算反馈（完成时间：2026-05-24；实现 commit `86ed42c`；执行方动态验收通过；独立审核 subagent 结论通过；PM 节点 C：通过）
- #8. 专项审核（完成时间：2026-05-24；审核方：`expert-agent-architect-opus47`；审核对象：HEAD `8445100` + 后续实现链路；结论：有条件通过，允许进入 PM #9；无 S0 / S1，遗留 5 条 S2）
- #9. PM 回归（完成时间：2026-05-24；回归对象：S2 收敛包 `50acb2f` + galaxyForesight 修复 `6111143`；自动检查 + Chrome headless 浏览器级回归通过；当前工具可覆盖部分关闭，人工体验与小屏可读性转 #10 制作人最终判断）

### 阻塞

暂无阻塞任务。

## 任务清单

### #1. PM 看板创建

- 状态：完成。
- 负责人：`/pm-qa-coordinator`。
- 目标：创建 `Workspaces/pm-qa-coordinator/v0.10.0-meta-progression-kanban.md`，作为 v0.10.0 高风险版本 PM 主记录。
- 优先级：P0。
- 依赖：制作人方向文档 `Workspaces/game-producer-lead-designer/v0.10.0-meta-progression-direction.md` 已提交，commit `52ae80d`。
- 允许修改范围：`Workspaces/pm-qa-coordinator/v0.10.0-meta-progression-kanban.md`。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、其他成员工作区、其他版本看板和方向文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 文件标题为 `# v0.10.0 局外持久化成长 PM 看板`。
  - 包含本轮目标、背景与制作人方向摘要、风险定级、严格范围边界、状态总览、任务清单、QA 回归点清单、高风险流程和后续维护规则。
  - 状态总览包含待办、进行中、待审核、完成、阻塞五个分组，且 #1 在完成、#2-#10 在待办。
  - 10 个任务均包含负责人、目标、优先级、依赖、允许修改范围、禁止修改范围、验收标准和风险。
  - `git status --short` 和最终提交范围显示 commit 仅包含本看板文件，不包含 `42.md`。
- 风险：当前仅完成计划创建，尚未完成 ADR、数值方案、UI 方案、核心实现、专项审核或 PM 回归。

### #2. 主程技术 ADR

- 状态：完成。
- 负责人：`/lead-game-programmer`。
- 目标：产出 v0.10.0 技术 ADR，明确 `SAVE_KEY` 策略、旧字段迁移、新 meta schema、`META_TALENT_TREE` 注册表、效果 helper、UI 状态机、测试钩子和回滚策略。
- 优先级：P0。
- 依赖：#1 PM 看板创建完成；制作人方向 commit `52ae80d`。
- 允许修改范围：`Workspaces/lead-game-programmer/v0.10.0-meta-progression-adr.md`。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、PM 看板、数值方案、UI 方案和制作人方向文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 明确保留现有 `SAVE_KEY` 还是新增 key，并说明旧 key 读取、写回、失败降级和回滚方式。
  - 明确旧 `mining / hull / weapons / unlocks.weaponFrame / unlocks.efficientCore` 到新 `talents` ranks 的映射规则。
  - 明确 `state.meta.points` 不丢失，旧字段存在时不报错，旧字段和新 talents 不产生双倍收益。
  - 明确 `META_TALENT_TREE` 字段集：`id / name / desc / category / tier / cost / maxRank / prereq / effects / uiHint`。
  - 明确 helper 边界：`getMetaTalentRank`、`hasMetaTalent`、`getMetaEffect`、`getSelectedStartProtocol`、`applyMetaStartProtocol`、`getMetaPurchaseState`、`purchaseMetaTalent`、`migrateMetaSave` 或等价 API。
  - 明确 Meta UI 状态机：面板打开、购买确认、点数不足、前置不足、满级、协议选择、结算提示和存档写入时机。
  - 明确测试钩子：重置 meta、注入旧存档、执行迁移、加点、购买节点、查询 rank / effect、选择协议、模拟结算、导出快照。
  - 明确回滚策略和迁移失败保护：优先保护玩家点数和已获得能力。
- 风险：ADR 若不先定 schema 和 helper，后续实现容易在 UI、结算和局内系统里散落硬编码；`SAVE_KEY` 决策不清会直接放大存档风险。
- PM 文档门槛审核（2026-05-24）：
  - 结论：通过。
  - 依据：ADR 已明确保留 `SAVE_KEY`、payload 内 `schemaVersion=2`、旧字段迁移、`META_TALENT_TREE / START_PROTOCOLS` 结构、集中 helper、UI 状态机、测试钩子、迁移失败保护和回滚策略。
  - 注意事项：#3 数值方案给出若干 ADR 增补口径，未阻塞 #5 启动，但 #5 T1 实现前必须同步采纳并在自检中说明。

### #3. 数值方案

- 状态：完成。
- 负责人：`/systems-numerics-designer`。
- 目标：产出首批天赋节点表、成本曲线、收益范围、开局协议收益、点数产出规则和平衡兜底，确保第一版可感知但不失控。
- 优先级：P0。
- 依赖：#1 PM 看板创建完成；需与 #2 ADR 对齐字段和 helper 口径，可与 #2 并行但必须在 #5 实现前定稿。
- 允许修改范围：`Workspaces/pm-qa-coordinator/v0.10.0-meta-progression-numerics.md`。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、其他成员工作区、PM 看板和制作人方向文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 给出资源型、防御型、武器型、探索型四条路线的首批节点表，字段匹配 ADR 中 `META_TALENT_TREE`。
  - 每个节点有稳定 `id`、成本、最大等级、前置条件、每级收益、解锁目标和 UI 提示。
  - tier 1 成本能支持 1-2 局内完成首次购买，tier 2 能形成开局协议短期目标。
  - 数值型节点每级收益默认控制在 5%-12%，若超出必须写明理由和回归点。
  - 明确点数产出来源、失败 / 通关差异、最低保底、上限或软上限策略。
  - 至少 3 个协议：采矿起步、防御起步、武器起步；默认 `balanced` 不需要解锁。
  - 明确平衡兜底：不得让初始资源、折扣、输出或耐久跳过核心采集、建造、研发、战斗和星系路径。
- 风险：跨局收益过强会让前期失去压力；成本过高会让失败后仍没有去处；协议收益若不可感知会无法支撑版本目标。
- PM 文档门槛审核（2026-05-24）：
  - 结论：通过。
  - 依据：数值方案已给出 12 节点表、四条路线、成本曲线、点数产出预期、5 个协议、effectKey 命名表、旧字段映射、平衡上限和 30 条 QA 回归清单。
  - ADR 增补点检查：旧 `weapons` 只映射到 `weaponCalibration`，不附送 `weaponEfficiency`；协议初始资源直接加到开局资源，不乘 `galaxyType.resourceMod`；`researchInsight` 只影响局内 `researchGain / state.resources.research` 获得量，不写局外研发或全局研发解锁；协议解锁沿用 `unlockProtocol` effectKey，并通过 `protocol` 字段声明目标协议。
  - 阻塞判断：以上增补点不阻塞 #5，但必须作为 #5 T1 的实现约束和自检项；若实现成员发现 ADR 示例与数值方案冲突，以本数值方案的定稿表为准，并在自检中记录同步情况。

### #4. UI / 信息架构方案

- 状态：完成。
- 负责人：`/ui-artist`。
- 目标：给出 Meta 面板、天赋树 / 卡片、购买反馈、开局协议选择和结算提示的信息架构方案。
- 优先级：P0。
- 依赖：#1 PM 看板创建完成；需参考 #2 ADR 和 #3 数值方案的字段，可并行草拟但必须在 #6 UI 实现前定稿。
- 允许修改范围：`Workspaces/ui-artist/v0.10.0-meta-progression-ui-plan.md`。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、PM 看板、ADR、数值方案和制作人方向文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 明确 Meta 入口位置、面板层级、点数显示、路线分组和节点卡片信息密度。
  - 节点状态覆盖可购买、点数不足、前置不足、已满级、已解锁协议和暂不支持重置。
  - 购买反馈包含确认、成功、失败原因、点数变化和下一步推荐。
  - 开局协议选择展示默认 `balanced` 与已解锁协议，并说明当前选择来源和下局影响。
  - 结算提示能显示本局获得点数、当前总点数、可购买推荐、差多少点、下一局建议，失败和通关都覆盖。
  - 文案清楚区分 v0.5 局内研发、v0.9 galaxyMap 和 v0.10 局外 Meta。
- 风险：现有右侧 UI 容量有限，若没有折叠和卡片优先级，天赋树、协议和结算提示会挤压战斗信息。
- PM 文档门槛审核（2026-05-24）：
  - 结论：通过。
  - 依据：UI 方案已覆盖入口改造、抽屉 / 小屏布局、节点卡信息密度、购买状态、协议页、结算局外成长区块、HUD 共存、术语边界、DOM / CSS 建议、键盘行为和 35 条 UI 回归点。
  - 注意事项：实现阶段必须移除或转接旧 5 个局外按钮，避免旧入口与新面板并存；所有协议影响文案必须写为“下一局”，不能暗示局中立即生效。

### #5. T1 存档迁移 + META_TALENT_TREE 注册表 + helper

- 状态：完成。
- 负责人：`/senior-systems-engineer`。
- 目标：实现新 meta schema、旧字段迁移、`META_TALENT_TREE` 注册表和集中 helper，为 UI、协议、结算和局内效果接入提供单一入口。
- 优先级：P0。
- 依赖：#2 主程 ADR、#3 数值方案、#4 UI / 信息架构方案均已完成并通过 PM 文档门槛。
- 允许修改范围：预计为 `Game/game.js`，如 ADR 明确需要极少量配套 DOM / 样式占位，可经 PM 更新后允许 `Game/index.html`、`Game/style.css`。
- 禁止修改范围：`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、所有工作区文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 空存档可生成完整默认 meta：`points / talents / selectedStartProtocol / migrationVersion`。
  - 旧存档缺 `talents` 时自动迁移，旧 `points` 原样保留。
  - 旧 `mining / hull / weapons / unlocks` 能映射到新 `talents` ranks，且不会和新 talents 双倍生效。
  - `META_TALENT_TREE` 集中定义节点，UI 和效果接入不散落硬编码节点数据。
  - helper 能覆盖 rank 查询、解锁判断、effect 查询、购买状态、购买执行、协议读取、协议应用和迁移。
  - 测试钩子能注入旧存档、执行迁移、读取 ranks / effects、设置点数和导出 meta 快照。
- 风险：这是 v0.10.0 的最高风险实现节点；若迁移失败、helper 边界不清或默认值不完整，会阻塞后续 UI 和回归。
- PM 启动条件（2026-05-24）：
  - 结论：可进入进行中。
  - T1 实现前必须同步采纳 #3 数值方案对 ADR 的增补口径：`startingCache rank>=1` 解锁 `miningStart`；`mul` 类 talent 支持 `perRankMul` 按 rank 累乘；`coreFortitude` 与 `hullIntegrity` 跨 effectKey 乘法叠加；旧 `weapons` 只映射 `weaponCalibration`，不附送 `weaponEfficiency`。
  - T1 实现时必须保持协议初始资源不乘 `galaxyType.resourceMod`，`researchInsight` 只影响局内 research 获得量，协议解锁 effectKey 使用 `unlockProtocol` + `protocol` 字段。
  - 若实现中发现 ADR、数值方案、UI 方案存在新冲突，暂停 #5 并回到 PM 看板登记阻塞原因。
- 实现记录（2026-05-24）：
  - T1 实现 commits：`cbf2dbd` + `ec9098f`。
  - T1 S1 修复 commit：`c2d20ab`。
  - 专项审核：`expert-agent-architect-opus47` 结论为有条件通过；S1-01 已在 `c2d20ab` 修复，审核说明修复后无需再次专项审核，可进入 PM 节点 A。
- PM 节点 A 回归（2026-05-24）：
  - 结论：通过，建议 #6 T2 进入进行中；T1 无 S0/S1 阻断。
  - 自动检查：`node --check Game/game.js` 通过。
  - 静态检查：`state.meta.mining`、`state.meta.hull`、`state.meta.weapons`、`state.meta.unlocks` 旧字段直接引用在 `Game/game.js` 中 0 命中。
  - VM 回归：通过内存 VM + DOM stub 执行 `window.__gameTest.meta`，未生成或提交临时验证脚本。
  - 旧存档迁移：`{ points: 7, mining: 2, hull: 1, weapons: 3, unlocks: { weaponFrame: true, efficientCore: true } }` 迁移到 schema v2 / migrationVersion v2；points 保留为 7；`weapons` 仅映射 `weaponCalibration=3`，`weaponEfficiency=0`；`weaponFrame / efficientCore` 正确映射为 rank 1。
  - 购买逻辑：`miningYield` 购买正确扣 1 点并升 rank；`startingCache` 在 `miningYield` rank 不足时返回 `prereq_unmet` 且不扣点；`miningYield` 达 maxRank 3 后第 4 次购买返回 `max_rank_reached` 且不扣点。
  - helper 效果：`getMetaEffect("miningYield")=1.2`、`hullIntegrity=1.2`、`weaponCalibration=1.3`、`researchInsight=1.24` 在指定 meta 快照下生效。
  - 旧系统边界：购买 `researchInsight` 不改变 v0.5 全局研究相关 `station.hullMod / station.weaponMod / station.techLevel / resources.research`；购买 meta 节点不改写 v0.9 `state.run.galaxyMap / currentGalaxyType / galaxyChoicesShown`。
  - 浏览器级检查：本节点未启动真实浏览器；覆盖范围为 `node --check`、静态搜索和 Node VM 行为验证。未覆盖真实浏览器 localStorage、真实 DOM 交互、渲染循环、手感、可读性和刷新重启主路径，需在 #6/#9 继续补齐。
  - 不阻塞项：旧 UI wrapper `buyTalent("hull")` 已不再即时调用 `improveStationHp(1.06)`；VM 中购买后现有 cell `maxHp/hp` 未即时变化，仅 `hullIntegrity` rank 生效。记录为 S2 行为差异，因旧入口将在 #6 被新 Meta 面板替换或转接，不阻塞 T1。
- 残留风险：
  - S2：旧 UI wrapper 行为差异可能影响玩家对旧按钮“立即变强”的预期；负责人 `/senior-gameplay-engineer`，下一步在 #6 新 Meta 面板中用明确文案和购买反馈承接“持续影响之后的局”。
  - S2：真实浏览器 localStorage 刷新 / 重启路径未覆盖；负责人 `/pm-qa-coordinator`，下一步在 #9 PM 回归用 Chrome / Edge 覆盖空存档、旧存档、购买、刷新、结算和重启。
  - S3：VM DOM stub 不能证明 UI 可读性、按钮状态视觉层级和结算引导体验；负责人 `/ui-artist` + `/pm-qa-coordinator`，下一步在 #6 实现后做浏览器级和人工体验验收。

### #6. T2 Meta UI 面板与购买逻辑

- 状态：完成。
- 负责人：`/senior-gameplay-engineer`。
- 目标：实现 Meta 面板、天赋节点展示、购买状态、购买确认 / 反馈和点数持久化写入。
- 优先级：P0。
- 依赖：#4 UI / 信息架构方案完成；#5 注册表、迁移和 helper 已完成并通过 PM 节点 A。
- 允许修改范围：预计为 `Game/game.js`、`Game/index.html`、`Game/style.css` 中与 Meta 面板、节点卡片、购买按钮和状态反馈相关区域。
- 禁止修改范围：`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、所有工作区文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - Meta 面板显示当前点数、路线分组、节点等级 / 最大等级、当前效果、下一级效果、成本和前置。
  - 可购买、点数不足、前置不足、已满级状态可被玩家一眼区分。
  - 购买通过 helper 完成，成功后立即更新 UI 和持久化存档，失败时显示明确原因。
  - 若 v0.10.0 不做重置，UI 明确提示“当前版本暂不支持重置”；若实现重置，必须有确认弹窗和测试钩子。
  - 旧“局外天赋按钮”被新面板替换或明确转接，不能出现两套购买入口。
  - 键鼠交互不阻塞既有战斗、任务、研发、星系图和结算 UI。
- 风险：购买写存档和 UI 状态同步容易出现点数显示滞后、重复扣点、按钮状态错误或旧入口重复。
- 实现记录（2026-05-24）：
  - T2 实现 commit：`3051614`。
  - 专项审核：首次结论未通过，发现 S1 问题：协议预览 / 选择会影响当前局建造成本边界，存在污染当前局 build cost 的风险。
  - T2 S1 修复 commit：`d9b9cfa`。
  - 专项复审：S1 已关闭，允许进入 PM 节点 B。
- PM 节点 B 回归（2026-05-24）：
  - 结论：通过，#6 可关闭；建议 #7 T3 开局协议 + 结算反馈进入进行中。当前未发现 S0 / S1 阻断。
  - 自动检查：`node --check Game/game.js` 通过。
  - 静态检查：`git diff --check` 通过；回归开始时 `git status --short` 仅显示 dirty `42.md`，未 stage 或提交。
  - Meta 面板 VM 回归：通过 `window.__gameTest.meta.renderMetaPanel("talents")` 等价路径验证面板可渲染 12 个节点；分类覆盖 resource / defense / weapon / exploration；节点快照包含 category、tier、rank、maxRank、nextCost、deficit、canBuy 和 reason。代码静态复核显示卡片渲染包含路线 / T 阶、等级、当前效果、下一级效果、升级消耗、前置不足、点数不足、满级和购买按钮状态。
  - 购买逻辑 VM 回归：`miningYield` 在 1 点时购买成功，扣 1 点、rank 变 1，并写入 `star-ring-station-meta-v1`；0 点购买 `hullIntegrity` 返回 `not_enough_points` 且不扣点；10 点但前置不足购买 `startingCache` 返回 `prereq_unmet` 且不扣点；`miningYield` 到 maxRank 3 后再次购买返回 `max_rank_reached` 且不扣点；购买后面板快照刷新为最新点数和 rank。
  - 旧入口与启动绑定：`Game/index.html` 中旧局外购买按钮已移除，仅保留 `metaPanelToggle` 和 `playersBtn`；VM 初始化覆盖 `initGame / createBuildUi / bind` 路径，无旧按钮缺失导致的启动绑定报错。
  - 文案边界：Meta 摘要和说明页明确区分“局外天赋点”与“局内科研点”；`researchInsight` 文案说明只提升局内科研点；耐久类天赋说明写明影响新建框架与后续新局，当前局已有结构耐久不会立即重算。
  - 协议边界 VM 回归：`__gameTest.meta.runProtocolSelectionBuildCostSelfCheck()` 通过；额外执行 `verifyProtocolSelectionDoesNotAffectCurrentRun()` 通过。选择 `weaponStart` / `miningStart` 只保存下一局协议，不改变当前局 `turret` / `mining` build cost、`resources.research`、`galaxyMap`、`objective` 和 `encounters`。
  - 未覆盖与风险：本节点未启动真实浏览器；未覆盖 Chrome / Edge 真实 localStorage 刷新重启、小屏布局、面板动效、真实鼠标点击路径和人工可读性 / 信息密度体验。以上记录为 S3 残留风险，不阻塞 #6 关闭，需在 #9 总回归继续覆盖。

### #7. T3 开局协议 + 结算反馈

- 状态：完成。
- 负责人：`/senior-gameplay-engineer`。
- 目标：实现默认与解锁协议的选择、下一局应用、结算推荐和失败 / 通关后的下一局目标提示。
- 优先级：P0。
- 依赖：#5 helper 完成；#6 Meta UI 面板能展示点数和节点状态；#3 数值方案明确协议收益。
- 允许修改范围：预计为 `Game/game.js`、`Game/index.html`、`Game/style.css` 中与开局协议选择、开局应用和结算反馈相关区域。
- 禁止修改范围：`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、所有工作区文档；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 默认 `balanced` 协议空存档可用，至少 3 个协议可由对应天赋解锁。
  - 同一局只能选择一个协议，协议在新局开始时应用，不允许局中临时切换。
  - 协议影响限制在初始资源、初始设施、初始蓝图折扣、早期提示或小幅倍率，不改写核心关卡结构。
  - 结算面板显示本局获得点数、当前总点数、可购买推荐、差多少点、下一局建议。
  - 失败与通关都走同一套反馈，只调整文案语气。
  - 协议和结算不污染 v0.5 研发系统、不改写 v0.9 galaxyMap 路径状态。
- 风险：协议应用时机若不清，会导致重开、刷新、局中切换或结算后立即生效等边界错误；结算推荐若不基于真实 purchase state，会误导玩家。
- 实现记录（2026-05-24）：
  - T3 实现 commit：`86ed42c`，提交信息 `完成v0.10.0 T3动态验收收尾`。
  - 执行方报告：动态验收通过。
  - 独立审核：审核 subagent 结论通过。
- PM 节点 C 回归（2026-05-24）：
  - 结论：通过，#7 可关闭；建议 #8 全量专项审核进入进行中。T3 局部实现审核和动态验收不能替代 v0.10.0 高风险版本的全量专项审核。
  - 自动检查：`node --check Game/game.js` 通过。
  - 静态检查：`git diff --check` 通过，无空白问题；回归开始时 `git status --short` 仅显示 dirty `42.md`，未 stage 或提交。
  - VM 回归：通过内存 VM + DOM/WebGL stub 执行 `window.__gameTest.meta.runProtocolSelectionBuildCostSelfCheck()`、`runStartProtocolApplicationSelfCheck()` 和 `verifyProtocolSelectionDoesNotAffectCurrentRun()`，未生成或提交临时验证脚本。
  - 当前局选择协议边界：选择 `weaponStart` / `miningStart` 只改变 `selectedStartProtocol`，不改变当前局 build cost、resources、`activeStartProtocolId`、`galaxyMap`、objective 或 encounters。
  - 新局协议应用：新局初始化后 `activeStartProtocolId` 正确变为所选协议；`miningStart` 初始资源为基础资源 +25 metal / +10 ore，未乘 `tradeHub` 等 `galaxyType.resourceMod`；采矿设施折扣生效，武器设施成本保持基线。
  - `getBuildCost` 边界：静态复核显示 `getBuildCost` 仅读取 `getActiveProtocolFacilityDiscount()` 的 active/applied protocol 运行态折扣，不读取 UI 当前 selected protocol；VM 用例 `buildCostReadsActiveProtocolOnly` 通过。
  - 结算反馈：`getMetaSettlementHints()` 返回本局 points gain、当前总点数、可购买建议和下一局协议建议；VM 快照为 `pointsGained=7`、`totalPoints=5`、`recommendedCount=3`、协议建议文案存在。结算路径只生成建议与提示，不调用 `purchaseMetaTalent()`，不自动购买。
  - 旧系统边界：VM 用例覆盖协议选择 / 新局应用前后 v0.9 `galaxyMap`、objective、encounters 语义稳定；`resources.research` 未被协议选择或协议初始资源污染。静态复核显示 v0.5 research unlock 仍由局内 `resources.research / station.*Mod / techLevel` 路径处理，T3 协议与结算未写入局外研发解锁。
  - 浏览器级检查：本节点未启动真实浏览器；覆盖范围为 `node --check`、`git diff --check`、静态复核和 Node VM 行为验证。
  - 人工体验检查：未覆盖真实鼠标点击、真实 localStorage 刷新 / 重启、Chrome / Edge 控制台、结算面板实际可读性、小屏布局、手感和玩家是否理解“下一局协议建议”。
- 残留风险：
  - S3：真实浏览器 localStorage、DOM 点击和控制台路径未覆盖；负责人 `/pm-qa-coordinator`，下一步在 #9 PM 回归用真实浏览器覆盖空存档、旧存档、协议选择、结算、刷新和重启。
  - S3：结算反馈可读性和下一局建议的玩家理解度未做人工体验；负责人 `/pm-qa-coordinator` + `/game-producer-lead-designer`，下一步在 #9 / #10 分别给出回归记录和产品接受结论。
  - S2：T1/T2/T3 均有局部审核，但尚未完成 v0.10.0 全量专项审核；负责人 `/lead-game-programmer` 或 `expert-agent-architect-opus47`，下一步进入 #8，统一检查存档安全、helper 边界、数值风险和 v0.1-v0.9 主路径污染。

### #8. 专项审核

- 状态：完成。
- 负责人：`/lead-game-programmer` 或 `expert-agent-architect-opus47`。
- 目标：对 v0.10.0 实现进行专项审核，重点检查需求满足、存档安全、helper 边界、数值风险和越界修改。
- 优先级：P0。
- 依赖：#5、#6、#7 完成并各自自检；相关执行提交已推送。
- 允许修改范围：审核阶段默认只允许新增 / 修改审核报告 `Workspaces/lead-game-programmer/v0.10.0-meta-progression-review.md` 或同等 PM 指定文件；如发现阻断问题，不直接修复，退回对应负责人。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、PM 看板和无关工作区文件；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 审核旧存档迁移、点数保留、旧字段映射、重复收益防护和迁移失败降级。
  - 审核 `META_TALENT_TREE` 是否集中定义，helper 是否是唯一效果接入口。
  - 审核购买、协议、结算是否满足制作人方向和 PM 验收。
  - 审核 v0.5 研发系统、v0.9 galaxyMap、v0.1-v0.9 关键路径是否存在污染风险。
  - 输出 S0/S1/S2/S3 问题分级、阻塞结论、允许进入 PM 回归与否。
- 风险：专项审核若只看功能表面，不查旧存档和 helper 调用边界，PM 回归阶段会很难定位持久化问题。
- 全量专项审核记录（2026-05-24）：
  - 审核方：`expert-agent-architect-opus47`。
  - 审核对象：HEAD `8445100` + 后续实现链路。
  - 结论：有条件通过，允许进入 PM #9；未发现 S0 / S1 阻断问题。
  - S2-01：`loadMeta` 遇到损坏存档时回落默认 meta，但未把损坏原文或恢复信息写入 `sessionStorage` 取证；影响为问题定位和玩家存档申诉证据不足。
  - S2-02：核心爆毁 `gameOver()` 仅 toast + 延迟 reload，缺少完整失败结算面板；影响为失败路径的局外成长反馈不如通关 / 守护者结算完整。
  - S2-03：`galaxyForesight` 候选面板提示未落地；影响为探索路线收益感知不足。
  - S2-04：`__gameTest.resetRun` 直接写 `state.meta.points = 0`，存在测试钩子误写盘风险；影响主要限测试 / 回归环境，但需避免真实玩家路径调用该钩子。
  - S2-05：混合存档同时含旧字段和 `talents` 时会跳过旧字段映射；影响为部分中间态 / 手工改档玩家的旧 `mining / hull / weapons / unlocks` 价值可能不被合并。
  - S3 残留：专项审核未新增 S3；此前 PM 节点记录的真实浏览器人工体验、小屏可读性、手感和刷新 / 重启主路径仍需在 #9 区分覆盖与未覆盖。
  - PM 判断：#8 完成，#9 可进入进行中；上述 S2 不阻塞自动 / 浏览器级回归，但必须在 #9 和 #10 中给出接受、延期或修复结论。
- S2 修复后专项复审记录（2026-05-24）：
  - 修复提交：S2 收敛包 `50acb2f`；`galaxyForesight` 修复 `6111143`。
  - 专项复审：`6111143` 通过，允许 PM #9 继续最终回归。
  - 修复前状态：#8 遗留 S2-01 损坏存档取证缺失、S2-02 失败结算面板缺失、S2-03 `galaxyForesight` 候选提示未落地、S2-04 `resetRun` 测试钩子误写盘风险、S2-05 混合存档旧字段未合并。
  - 修复后状态：PM #9 通过 `runMetaS2RegressionSelfCheck()` 复测 6/6，以上 S2 在自动 / headless 工具覆盖范围内关闭；真实人工体验、小屏布局和长期节奏仍作为 #10 产品接受风险。

### #9. PM 回归

- 状态：完成。
- 负责人：`/pm-qa-coordinator`。
- 目标：执行 v0.10.0 回归，覆盖旧存档、购买、重启、结算、开局协议、v0.5 研发边界、v0.9 galaxyMap 边界和 v0.1-v0.9 不回潮。
- 优先级：P0。
- 依赖：#8 专项审核通过或有条件通过；阻断问题已修复并复测。
- 允许修改范围：仅允许在本看板追加回归结论、缺陷记录、验证证据和状态变化；必要时可新增 `Workspaces/pm-qa-coordinator/v0.10.0-meta-progression-regression.md`，需先更新看板范围。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`持续开发Prompt.md`、`Releases/`、其他成员工作区；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 自动检查、浏览器级检查、人工体验、未覆盖路径和是否阻塞接受分别记录。
  - 按 QA 回归点清单覆盖旧存档、迁移、点数、购买、协议、结算、localStorage、刷新重启和旧版本关键路径。
  - 缺陷记录包含复现步骤、实际结果、期望结果、严重程度、影响范围、负责人和复测结论。
  - 修复后的缺陷按原复现步骤复测，并说明失败路径是否关闭。
  - 给出 PM 是否建议进入制作人最终决定的结论。
- 风险：真实浏览器 / localStorage 行为和测试钩子行为可能不一致；需要保留未覆盖路径，不能用 headless 或脚本结果替代手感与可读性判断。
- PM #9 最终回归记录（2026-05-24，S2 修复后）：
  - 当前结论：完成；S2 收敛包 `50acb2f` + `galaxyForesight` 修复 `6111143` 后，自动检查、Chrome headless 浏览器级 DOM 烟测和 `__gameTest.runMetaS2RegressionSelfCheck()` 均通过。PM 判断当前工具可覆盖部分足以关闭 #9，并建议 #10 进入进行中；真实人工体验、小屏布局、手感、信息密度和玩家理解度不作为 #9 阻塞，转入 #10 制作人最终接受风险。
  - 自动检查：`node --check Game/game.js` 通过；`git diff --check` 通过；回归开始时 `git status --short` 仅显示 dirty `42.md`，未 stage 或提交。
  - S2 自检：Chrome headless 中执行 `window.__gameTest.runMetaS2RegressionSelfCheck()` 通过，结果 `ok=true`，6/6 用例通过：`corruptMetaFallbackStoresRaw`、`mixedMetaMigrationMergesLegacyFields`、`gameOverShowsFailureSettlementPanel`、`galaxyForesightAddsHintsWithoutRngChange`、`resetRunDoesNotOverwriteStoredMetaPoints`、`v05AndV09BoundariesNotPolluted`。
  - 存档迁移：旧存档 `{ points: 7, mining: 2, hull: 1, weapons: 3, unlocks: { weaponFrame: true, efficientCore: true } }` 迁移通过，points 保留 7，`miningYield=2 / hullIntegrity=1 / weaponCalibration=3 / weaponFrame=1 / efficientCore=1`，且 `weaponEfficiency=0`，确认旧 `weapons` 不附送 `weaponEfficiency`。
  - 混合存档：同时含 `talents.weaponEfficiency=1` 和旧 `mining / hull / weapons / unlocks` 时迁移合并通过，points 保留 9，旧字段合并到 `miningYield=3 / hullIntegrity=2 / weaponCalibration=2 / weaponFrame=1 / efficientCore=1`，原有 `weaponEfficiency=1` 保留但不由旧 `weapons` 额外赠送。
  - 损坏存档 fallback：损坏 localStorage JSON 可回退默认 meta，fallback points 为 0，并在 `sessionStorage` 记录 `META_CORRUPT_RAW_KEY` 取证信息；该路径关闭原 S2-01。
  - 点数不丢：`resetRunDoesNotOverwriteStoredMetaPoints` 通过，持久化 points 37 在 `resetRun` 后仍为 37；购买覆盖中 100 点起步完成依赖链购买后剩余 70 点，未发现异常扣点。
  - Meta 面板和 12 节点购买：headless 打开 `Game/index.html` 后 `#metaPanelToggle` 可打开面板，talents 页 DOM 观察到 12 个 `.meta-node-card`；按依赖链购买 12 个节点均成功，覆盖 `miningYield / metalRefining / startingCache / hullIntegrity / coreFortitude / buildDiscount / weaponCalibration / weaponFrame / weaponEfficiency / researchInsight / efficientCore / galaxyForesight`。
  - 协议 selected vs active 与新局 apply：`runStartProtocolApplicationSelfCheck()` 通过，选择 `weaponStart / miningStart` 只改变 selected，不改变当前局 build cost、resources、active protocol、`galaxyMap`、objective 或 encounters；新局后 `miningStart` active 生效，初始资源 +25 metal / +10 ore，采矿设施折扣生效，武器设施成本保持基线。协议页 DOM 观察到 5 个 `.meta-protocol-card`，`balanced / miningStart / defenseStart / weaponStart / scoutStart` 均可展示。
  - 结算反馈与失败结算：`settlementHintsPreview` 路径返回本局 points gain、当前总点数、可购买推荐和下一局协议建议；`gameOverShowsFailureSettlementPanel` 通过，失败时 `runSettlementPanel` 可见，标题为“失败结算”，文案包含核心爆毁提示，重新开始按钮可见，停留按钮隐藏。
  - `galaxyForesight` 候选提示：`galaxyForesightAddsHintsWithoutRngChange` 通过；开启前后候选列表 JSON 完全一致，未改变 RNG / 候选结果；开启后候选卡片出现 `galaxy-map-foresight` 提示并包含“星图洞察”标题，关闭原 S2-03。
  - v0.5 / v0.9 边界：`v05AndV09BoundariesNotPolluted` 通过；`researchInsight` 仍只影响局内 research 获得倍率，不写 `resources.research / station.hullMod / station.weaponMod / station.techLevel`；`runGalaxyJumpStateMachineSelfCheck()` 通过 stale confirm、无 pending 拒绝、取消重开、确认后重开、终局单次推进等检查，Meta 购买 / 协议选择不污染 `galaxyMap / objective / encounters`。
  - 浏览器级环境：本机可用 Chrome headless，使用 DevTools 协议直接打开 `Game/index.html`；`document.readyState=complete`，`typeof __gameTest=object`，`#game` canvas、`#moreStatusPanel`、`#metaPanelToggle`、`#runSettlementPanel`、`#metaPanel` 均存在。DevTools 采集范围内 `Runtime.exceptionThrown` 和 `Log.entryAdded` 均为空；因损坏存档 fallback 自检触发了预期 console warning `[meta] 检测到损坏存档，已回退默认 meta。`。
  - 未覆盖：真实 Chrome / Edge 有头人工操作、小屏布局、鼠标点击完整路径、Meta 面板信息密度、失败后的玩家理解、开局协议手感和长期成长节奏。以上不阻塞 #9 工具回归完成，但必须由 #10 制作人最终决定给出接受、延期或继续修复结论。
  - 残留风险：headless 结果不能替代人工体验；本轮未提交临时脚本，未修改 `Game/`、`42.md`、`Docs/`、`Releases/` 或其他工作区。

### #10. 制作人最终决定 / 发布判断

- 状态：进行中。
- 负责人：`/game-producer-lead-designer`。
- 目标：基于专项审核和 PM 回归决定 v0.10.0 是否接受、是否发布、是否需要继续削范围或追加修复。
- 优先级：P0。
- 依赖：#9 PM 回归已完成；无 S0/S1 阻断问题；S2 自动 / headless 覆盖范围内已复测关闭；真实人工体验、小屏可读性和产品接受风险待制作人决定。
- 允许修改范围：`Workspaces/game-producer-lead-designer/v0.10.0-meta-progression-acceptance.md`；若决定发布，后续由 `/it-engineer` 按发布规则处理 `Releases/<version>/` 和 tag。
- 禁止修改范围：`42.md`、`持续开发Prompt.md`、`.cursor/`、无关工作区文件；未经发布决定不得修改 `Releases/`、不得打 tag；不得删除文件；不得 add / commit / stash / 恢复 `42.md`。
- 验收标准：
  - 制作人明确玩家收益是否成立：失败也有去处、路线选择明确、协议可感知、结算能导向下一局。
  - 明确旧存档迁移、Meta UI 可读性、开局协议体验和 v0.5 / v0.9 边界是否可接受。
  - 明确是否发布 v0.10.0；若不发布，说明必须修复项和下一轮目标。
  - 若发布，进入发布流程并由 PM 维护发布检查清单。
- 风险：即使技术回归通过，若玩家读不懂路线或协议收益不可感知，制作人可能不接受发布。

## QA 回归点清单

### 存档与迁移

- 旧 localStorage 中只有 `points / mining / hull / weapons / unlocks` 时，进入游戏后自动补齐 `talents / selectedStartProtocol / migrationVersion`。
- 旧 `points` 数值迁移后不减少、不清零、不被重复结算覆盖。
- 旧 `mining / hull / weapons` 映射到对应新 talent rank，刷新后 rank 保持。
- 旧 `unlocks.weaponFrame / unlocks.efficientCore` 映射到对应解锁节点，刷新后仍可用。
- 旧字段和新 `talents` 同时存在时，效果只按 helper 口径计算一次，不产生双倍收益。
- 空存档首次进入游戏可正常生成默认 meta，默认协议为 `balanced`。
- 损坏或缺字段 meta 不导致游戏启动崩溃，点数保护策略符合 ADR。
- `SAVE_KEY` 策略符合 ADR：保留原 key 或执行明确迁移，浏览器 localStorage 中无幽灵 key 造成的读写混乱。

### 天赋购买与效果 helper

- 节点购买消耗正确点数，点数不足时不可购买且提示明确。
- 前置不足时不可购买，提示缺少的节点和 rank。
- `maxRank` 达到上限后不可继续购买，不重复扣点。
- 多级成本按数值方案计算，刷新后当前等级和剩余点数一致。
- `getMetaTalentRank / hasMetaTalent / getMetaEffect` 或等价 helper 能返回正确值。
- 采矿、耐久、武器、科研、初始资源、解锁项等效果均通过 helper 接入。
- 购买后 UI、存档和 helper 查询结果一致。

### 开局协议

- 默认 `balanced` 协议无需解锁，空存档可正常开局。
- 采矿起步、防御起步、武器起步至少 3 个协议按对应天赋解锁。
- 未解锁协议不可选择，原因提示明确。
- 同一局只能选择一个协议，选择后刷新仍保持。
- 新局开始时协议按 ADR 时机生效，局中不能临时切换改变当前局状态。
- 协议收益符合数值方案，不绕过核心采集、建造、研发、战斗或星系路径。

### 结算与玩家引导

- 失败结算显示本局获得局外点数、当前总点数、可购买推荐或差多少点。
- 通关结算显示本局获得局外点数、当前总点数、可购买推荐或下一局建议。
- 结算推荐基于真实购买状态，不推荐已满级或前置不可能满足的节点。
- 结算后进入 Meta 面板能看到点数变化并可购买符合条件的节点。
- 重启浏览器后结算获得点数、已购节点和已选协议仍持久化。

### 旧系统边界

- v0.5 研发系统仍是本局内临时成长，结算后不被写入局外 meta。
- UI 文案不把 research / 研发称为局外永久天赋，不把 Meta 节点称为局内研究。
- v0.9 `galaxyMap` 的路径、候选、跃迁状态机不被 Meta 购买或协议选择改写。
- v0.9 星系选择仍能正常打开、取消、确认和记录路径。
- v0.1-v0.4 基础采集、建造、任务、敌人波次和奖励主路径无回潮。
- v0.6 hostile-station、v0.7 锁敌 / 齐射、v0.8 随机事件主路径无回潮。

### 浏览器与 localStorage

- Chrome / Edge 至少一个真实浏览器完成空存档、旧存档、购买、刷新、结算、重启主路径。
- localStorage 手动注入旧 meta 后，迁移结果可通过测试钩子或 UI 观察验证。
- 清空 localStorage 后新玩家路径可正常开局、结算和购买。
- 多次刷新不重复迁移、不重复赠点、不重复扣点。
- 控制台无启动错误、购买错误、结算错误和未捕获 promise / runtime error。

## 高风险流程

1. 制作人方向：`Workspaces/game-producer-lead-designer/v0.10.0-meta-progression-direction.md` 已提交，commit `52ae80d`。
2. PM 看板：本文件建立任务、验收、范围和回归清单。
3. ADR / 数值 / UI 方案：#2、#3、#4 必须先完成并通过 PM 文档门槛。
4. 实现：按 #5、#6、#7 分批进入 `Game/`，不得在方案完成前提前实现。
5. 专项审核：#8 检查需求、存档、helper、数值、UI 和越界修改。
6. PM 回归：#9 覆盖旧存档、购买、重启、结算、协议、v0.5 研发、v0.9 galaxyMap 和 v0.1-v0.9 不回潮。
7. 制作人决定：#10 判断接受、延期、削范围、修复或发布。

## 缺陷记录格式

```markdown
### BUG-编号：标题

- 严重程度：S0 / S1 / S2 / S3
- 影响范围：
- 负责人：
- 前置条件：
- 复现步骤：
- 实际结果：
- 期望结果：
- 初次发现版本 / commit：
- 修复版本 / commit：
- 复测步骤：
- 复测结论：
- 残留风险：
```

## 后续维护规则

- 每个任务状态变化必须同步状态总览和任务清单，避免总览与明细不一致。
- 执行成员完成任务后必须说明修改范围、验证方式和残留风险，并按成员粒度提交。
- PM 回归结论必须区分自动检查、浏览器级检查、人工体验、未覆盖路径和是否阻塞接受。
- 发现越界修改、`42.md` 被 stage、存档点数丢失或 v0.9 galaxyMap 污染时，直接标为阻塞并退回对应负责人。
- `42.md` 是用户 dirty 改动，任何阶段都不得提交、恢复、覆盖、stash 或重写。
