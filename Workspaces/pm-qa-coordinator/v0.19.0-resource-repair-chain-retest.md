# v0.19.0-resource-repair-chain #6 原路径复测

## 基本信息

- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 01:32:56 +08:00。
- 入口：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 浏览器：可见 Chrome + CDP，端口 `9999`。
- 临时 profile：`C:\Users\ylswd\AppData\Local\Temp\agentgame-v019-retest-20260610-0155-9999`。
- 输入方式：DOM 选择建造按钮 + CDP `Input.dispatchMouseEvent` 鼠标点击画布网格。
- 原始 JSON 摘要：`Workspaces/pm-qa-coordinator/v0.19.0-resource-repair-chain-retest-20260610-0132-raw.json`。
- 边界：未调用 `buildAt`，未直接修改 `state` / storage，未调用 `resetRun`，未清空或写入用户真实存档。

## 环境与存档

- `localStorageLength=0`、`sessionStorageLength=0`、`localKeys=[]`、`sessionKeys=[]`。
- runtime errors：`0`。
- console errors / unhandled rejections：`0`。
- 浏览器页加载后到脚本暂停前自然运行约 30 秒，资源有核心自然补给；全程使用临时 profile，不影响用户真实存档。

## 原路径建造链

| 步骤 | gridKey | 结果 | 资源摘要 | 点击对齐 |
| --- | --- | --- | --- | --- |
| `frame -> power` | `-1,-1` | 建成 `power` | `metal 155.89 -> 147.89 -> 125.89`，`ore 65.48 -> 57.48` | `aligned=true`，`screenDelta.distance=0` |
| `frame -> mining` | `-1,1` | 建成 `mining` | `metal 125.89 -> 117.89 -> 89.89`，`ore 57.48 -> 47.48` | `aligned=true`，`screenDelta.distance=0` |
| 10 秒等待 | `-1,1` | `mining` 仍未采集 | `metal 89.89 -> 98.58`，`ore 47.48 -> 49.32`，`gas 38.65 -> 39.88`，`plasma 8.76 -> 9.02` | 资源增长不可归因采矿 |
| `frame -> turret` | `-2,0` | 建成 `turret` | `metal 98.58 -> 90.58 -> 55.58`，`ore 49.32 -> 37.32` | `aligned=true`，`screenDelta.distance=0` |
| 45 秒等待 | `-2,0` | 补足 repair 资源 | `metal 55.58 -> 104.11`，`ore 37.32 -> 45.48`，`gas 39.88 -> 46.82`，`plasma 9.02 -> 10.15` | 资源增长不可归因采矿 |
| `frame -> repair` | `0,-2` | 建成 `repair` | `metal 104.11 -> 96.11 -> 52.11`，`plasma 10.15 -> 4.15` | `aligned=true`，`screenDelta.distance=0` |
| repair 后 20 秒等待 | `-1,1` | `mining` 仍未采集 | `metal 52.11 -> 79.32`，`ore 45.48 -> 49.13`，`gas 46.82 -> 50.75`，`plasma 4.15 -> 4.66` | `harvestingCount=0`，不可归因采矿 |

## Repair 复测结论

`repair` 在原路径 `0,-2` 正常建成，最终 `0,-2 facility=repair`。建成时资源从 `metal 96.11 / plasma 10.15` 扣到 `metal 52.11 / plasma 4.15`，与 `repair` 成本 `metal 44 / plasma 6` 一致。

建成后 toast 为 `维修站已建造。`。当前仍选中 `repair` 时，statusAlerts 显示：`当前仍选中「维修站」：下一座还缺 等离子 2（不是刚才建造失败；核心会缓慢产金属）`。该提示已明确区分“刚才 repair 成功”和“下一座 repair 预算不足”，#5 的最小提示修复关闭了原先误读点。

## Mining 覆盖与收益

- 矿站位置：`-1,1`，`facility=mining`，最终 `active=true`。
- `getResourceReachability().overall`：`minerCount=1`、`activeCount=1`、`harvestingCount=0`、`outOfRangeCount=1`、`allOutOfRange=true`、`reasonKey=all_miners_out_of_range`。
- 矿站条目：`cellKey=-1,1`、`target=null`、最近资源为 `行星-1 / ore`，`distance=354.49`、`range=228.13`、`gap=127`、`inRange=false`、`reasonKey=out_of_range`。
- 可见 UI 文案：`采矿站待命：距「行星-1」还差 138，请进入彩色外环`、`采矿站未覆盖资源`。
- resourceGuide 文案包含：`距离 365 / 采矿范围 228 · 还需靠近 138`、`采矿站就绪，请进入「行星-1」的彩色外环（还差 138）`。

10 秒、45 秒、repair 后 20 秒等待窗口内资源均有自然增长，但 `harvestingCount=0`、`target=null`、`allOutOfRange=true`，因此不能归因于采矿收益。v0.18 原 `mining` 未覆盖资源外环 / 收益未形成失败点仍未关闭。

## Toast / StatusAlerts 摘要

- repair 成功 toast：`维修站已建造。`
- repair 后下一座预算提示：`当前仍选中「维修站」：下一座还缺 等离子 2（不是刚才建造失败；核心会缓慢产金属）`
- mining 覆盖提示：`采矿站待命：距「行星-1」还差 138，请进入彩色外环`
- mining 状态提示：`采矿站未覆盖资源`
- 建造模式提示：`建造模式：维修站 — 点网格或 Enter 放置；Esc / 「指针/航行」退出`

## PM 结论

- #6 `repair` 原路径复测：通过。原资源不足失败点已通过等待补资源关闭，且 #5 后提示不再把下一座资源不足误读成刚才建造失败。
- #6 `mining` 原路径复测：未通过收益闭环。矿站建成但仍未覆盖 `行星-1` 资源外环，`harvestingCount=0`，等待窗口资源增长不能归因采矿。
- #7 交火 / Meta 解锁判断：继续阻塞。自然交火、战后复盘和 Meta 回流不建议解锁，直到出现可归因采矿收益路径，或制作人明确接受“repair 闭环、mining 继续补证”的阶段性边界。

## 覆盖与残留风险

- 自动检查：runtime errors、console errors、storage signature、只读 hook 字段已覆盖。
- 浏览器级检查：可见 Chrome + CDP 鼠标点击覆盖。
- 人工体验：未覆盖真实人工鼠标和真实触控。
- 未覆盖路径：采矿收益成功路径、真实触控、自然交火、战后复盘、Meta 回流、专项审核。
- 阻塞接受：`repair` 不阻塞；`mining` 收益仍阻塞交火 / Meta 解锁。
