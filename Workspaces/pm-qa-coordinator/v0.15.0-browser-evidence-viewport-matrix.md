# v0.15.0 #6 三视口 / 小屏 / 复制摘要 / 触控假设证据

## 证据概览

- 任务关联：#6 T3 三视口/小屏/复制摘要/触控假设证据。
- 记录人：`/pm-qa-coordinator` + `/ui-artist`。
- 时间：2026-06-09 23:07 +08:00。
- 环境：Windows 10.0.26200；Chrome/148.0.7778.217；可见 Chrome app window + CDP `Runtime.evaluate`。
- URL：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 设置方式：每个视口用独立可见 Chrome app window 启动，命令行 `--window-size` 请求 `760x640`、`420x740`、`390x844`；实际 `innerWidth/innerHeight` 以 CDP 读取为准。
- 原始 JSON：`Workspaces/pm-qa-coordinator/v0.15.0-browser-evidence-viewport-matrix-20260609-230716.json`。
- 真实触控：未覆盖。`maxTouchPoints=0`，`pointerCoarse=false`，鼠标/CDP 证据不等于真实触控通过。

## 三视口矩阵

| 请求视口 | 实际 viewport / DPR | Release candidate panel | Build panel | HUD / 更多状态 | 关键按钮 | Galaxy / Meta / 复盘 | P0 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `760x640` | `747x605` / DPR `2` | 可见；`731x254`；可滚动；状态仍显示 `正在运行 runReleaseCandidateChecks...` | 可见；可滚动；首个建造按钮可点 | 状态面板可见且可滚动；`更多状态`展开后落到视口下方，Meta/players 入口不在当前可视区 | `复制摘要`可点；首个建造按钮可点；`更多状态`/Meta 当前不可点 | 通过 test hook 读取到复盘/结算文本，但按钮被下方面板/视口遮挡；Galaxy/Meta 在该强制状态未形成可点通过证据 | 记录完成；存在小屏/面板叠加非阻塞 polish；不能判定 P0 全通过 |
| `420x740` | `407x705` / DPR `2` | 可见；`391x268`；可滚动；自动检查完成 `automatic 11/11 pass`、`manual uncovered 8` | 可见；可滚动；首个建造按钮可点 | 状态面板可见且可滚动；`更多状态`展开后可读但中心 hit 被画布/叠层影响 | `复制摘要`可点；首个建造按钮可点；Meta 入口需滚动到状态面板深处 | Meta 抽屉全屏可见且 body 可滚动、关闭可点；Galaxy 面板可见且内容可滚动、取消按钮可点；复盘/结算面板可滚动但开始新局按钮中心 hit 被遮挡 | 三视口浏览器证据通过；复盘按钮可点性在强制叠层状态未通过 |
| `390x844` | `377x809` / DPR `2` | 可见；`361x307`；可滚动；自动检查完成 `automatic 11/11 pass`、`manual uncovered 8` | 可见；可滚动；首个建造按钮可点 | 状态面板可见且可滚动；更多状态 body 可滚动；部分中心 hit 被建造/画布区域影响 | `复制摘要`可点；首个建造按钮可点；Meta 入口需滚动 | Meta 抽屉全屏可见且 body 可滚动、关闭可点；Galaxy 面板可见且内容可滚动、取消按钮可点；复盘/结算面板可滚动但开始新局按钮中心 hit 被遮挡 | 三视口浏览器证据通过；复盘按钮可点性在强制叠层状态未通过 |

## 复制摘要

- `760x640`：`copyReleaseCandidateSummary()` 返回 `ok=true`、`method=execCommand`，即 textarea / `document.execCommand("copy")` fallback 路径。
- `420x740`：返回 `ok=true`、`method=clipboard`。
- `390x844`：返回 `ok=true`、`method=clipboard`。
- 摘要字段：三视口均包含 browser、viewport、runtimeErrors、storage hash 字段。
- 风险：机器检查字段 `includesManual=false`，原因是摘要标题为 `[人工结论 — 脚本不自动标记通过]`，不包含脚本检索的 `[人工结论` 精确片段；人工状态内容仍在页面摘要文本中。建议 #8 复核复制摘要字段口径。

## 触控假设

- 未使用真实触控设备；本次输入属于可见 Chrome + CDP / 鼠标语义。
- 不得写“移动端触控通过”。
- 触控残留风险：小屏滚动、真实手指误触、触摸惯性、移动浏览器 UI chrome、软键盘均未覆盖。

## 缺陷 / 风险分级

- 阻塞：无新增 S0/S1 代码阻塞证据。
- 未覆盖：真实触控、完整真人手玩、自然交火后的武器/护盾/维修、战斗后复盘和 Meta 回流仍未完整覆盖。
- 非阻塞 polish：Release candidate panel 打开时会压住状态面板中心 hit；小屏 `更多状态`/Meta 入口在展开后需要滚动较深；强制结算面板与建造/状态面板共存时，开始新局按钮中心 hit 被其他层覆盖。

## 结论

#6 三视口/小屏/复制摘要证据已完成记录，但不能作为移动端完整通过或 P0 人工路径完整通过。#9 不得建议直接进入发布准备；建议先进入 #8 专项审核，并把真实触控、真人交火复盘、Meta 回流点击和小屏叠层 polish 作为残留风险。
