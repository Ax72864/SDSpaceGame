# v1.0.0 #6 首局 P0 建造 / 采矿 / 资源 / 维修闭环复测

## 基本信息

- 版本名：`v1.0.0-release-candidate`
- 任务：PM 看板 #6 首局 P0 路径建造 / 采矿 / 资源 / 维修闭环复测
- 复测时间：2026-06-10 03:42-03:46 UTC+8
- 页面：`Game/index.html?playtest=1`
- 浏览器：可见 Chrome + CDP，独立临时 profile
- 证据 JSON：`Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-retest.json`
- 禁止项确认：未修改 `Game/`；未调用 `buildAt`；未直接修改 `state`、resources、storage 或 body amount；只读 hook 仅用于采样。

## 已读取依据

- `Workspaces/pm-qa-coordinator/v1.0.0-release-candidate-kanban.md` #6 / #7
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-entry-contamination-triage.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-evidence.md`
- `Game/game.js` 中 playtest 入口默认暂停、采矿引导、release candidate panel、playtest 只读 hook

## 复测路径

1. 使用全新临时 profile 打开 `file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
2. 采样入口状态、storage signature、runtime errors。
3. 真实点击 release candidate panel 的「关闭」按钮。
4. 真实点击「继续」推进；第二轮为稳定网格点击，又真实点击「暂停」后执行建造输入。
5. 按采矿引导 ghost path 逐格点击建造框架，再尝试 `mining` / `power` / `turret` / `repair`。
6. 等待采矿窗口，记录资源、toast、statusAlerts、runtime errors、storage signature。

## 关键证据

| 项目 | 结果 |
| --- | --- |
| 入口默认暂停 | 通过：首帧 `paused=true`，按钮文案为「继续」。 |
| 初始结算污染 | 通过：首帧 goal 不是「查看本局结算」，`runSettlementOpen=false`，`objective.failed=false`。 |
| 初始 storage | 通过：全新临时 profile 下 `save.hasValue=false`，`corruptRaw.hasValue=false`。 |
| runtime errors | 通过：最终 `runtimeErrorCount=0`。 |
| 建造链 | 未通过：两轮 CDP 鼠标路径均未形成 `mining` / `power` / `turret` / `repair` 的有效设施闭环。 |
| 采矿收益 | 未通过：最终 `harvestingCount=0`，未取得目标资源下降或收益归因证据。 |
| 资源不足提示 | 未通过：只取得框架占用 / 当前资源不足引导文本，未覆盖有效设施资源不足路径。 |
| 维修站反馈 | 未通过：维修站未建成，repair summary 仍为 `repair_no_target` / `repairerCount=0`。 |

## 复测细节

### Attempt 1

- 方式：点击「继续」后直接按采矿引导路径建造。
- 结果：入口验证通过，但首个 ghost path 目标点击后 toast 为「框架必须连接到核心或已连接结构」；后续未建成采矿站。
- 判断：该样本不能作为 P0 建设链通过证据。

### Attempt 2

- 方式：点击「继续」后再点击「暂停」稳定画面，按 ghost path 建造框架，再尝试设施建造。
- 结果：入口验证通过，storage 仍为空，runtime errors 为 0；但最终关键格仍为 `frame`，未升级为 `mining` / `power` / `turret` / `repair`。
- 代表性记录：
  - `targetGridKey=11,-6`
  - `powerKey=2,0`
  - `turretKey=3,0`
  - `repairKey=4,0`
  - 最终 resources：`metal=11`、`ore=63`、`gas=37`、`plasma=8`、`powerAvailable=6`
  - 最终 `harvestingCount=0`

## PM 结论

#6 仍未通过，继续阻塞 #7。

本轮可以关闭旧的「playtest 入口自动结算 / storage 污染」疑点：全新临时 profile 首帧已默认暂停、非结算态、storage 为空。但首局 P0 建设链仍没有取得有效通过证据，采矿收益、资源不足提示、维修站建成与反馈均未闭环。

## 残留风险与下一步

- 风险：真实玩家路径或 CDP 可见鼠标路径下，建造选择 / 网格点击仍可能丢失或误命中；需要玩法 / 系统工程继续排查设施选择与建造输入可验证性。
- 风险：#5 已证明单独采矿收益可归因，但 #6 首局建设链仍不能证明玩家能从空档自然走到采矿、资源规划和维修。
- 下一步：#7 自然交火 / 防御 / 维修 / 战后复盘继续保持阻塞；应先由执行成员修复或提供可重复的真实输入建造路径，再由 PM 复测 #6。
