# v1.0.0 #6 首局 P0 建造 / 采矿 / 资源 / 维修闭环复测

## 基本信息

- 版本名：`v1.0.0-release-candidate`
- 任务：PM 看板 #6 首局 P0 路径建造 / 采矿 / 资源 / 维修闭环复测
- 复测时间：2026-06-10 03:42-03:46 UTC+8
- 页面：`Game/index.html?playtest=1`
- 浏览器：可见 Chrome + CDP，独立临时 profile
- 证据 JSON：`Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-retest.json`
- 禁止项确认：未修改 `Game/`；未调用 `buildAt`；未直接修改 `state`、resources、storage 或 body amount；只读 hook 仅用于采样。

## 已读取依据

- `Workspaces/pm-qa-coordinator/v1.0.0-release-candidate-kanban.md` #6 / #7
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-entry-contamination-triage.md`
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-evidence.md`
- `Game/game.js` 中 playtest 入口默认暂停、采矿引导、release candidate panel、playtest 只读 hook

## 复测路径

1. 使用全新临时 profile 打开 `file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
2. 采样入口状态、storage signature、runtime errors。
3. 真实点击 release candidate panel 的「关闭」按钮。
4. 真实点击「继续」推进；第二轮为稳定网格点击，又真实点击「暂停」后执行建造输入。
5. 按采矿引导 ghost path 逐格点击建造框架，再尝试 `mining` / `power` / `turret` / `repair`。
6. 等待采矿窗口，记录资源、toast、statusAlerts、runtime errors、storage signature。

## 关键证据

| 项目 | 结果 |
| --- | --- |
| 入口默认暂停 | 通过：首帧 `paused=true`，按钮文案为「继续」。 |
| 初始结算污染 | 通过：首帧 goal 不是「查看本局结算」，`runSettlementOpen=false`，`objective.failed=false`。 |
| 初始 storage | 通过：全新临时 profile 下 `save.hasValue=false`，`corruptRaw.hasValue=false`。 |
| runtime errors | 通过：最终 `runtimeErrorCount=0`。 |
| 建造链 | 未通过：两轮 CDP 鼠标路径均未形成 `mining` / `power` / `turret` / `repair` 的有效设施闭环。 |
| 采矿收益 | 未通过：最终 `harvestingCount=0`，未取得目标资源下降或收益归因证据。 |
| 资源不足提示 | 未通过：只取得框架占用 / 当前资源不足引导文本，未覆盖有效设施资源不足路径。 |
| 维修站反馈 | 未通过：维修站未建成，repair summary 仍为 `repair_no_target` / `repairerCount=0`。 |

## 复测细节

### Attempt 1

- 方式：点击「继续」后直接按采矿引导路径建造。
- 结果：入口验证通过，但首个 ghost path 目标点击后 toast 为「框架必须连接到核心或已连接结构」；后续未建成采矿站。
- 判断：该样本不能作为 P0 建设链通过证据。

### Attempt 2

- 方式：点击「继续」后再点击「暂停」稳定画面，按 ghost path 建造框架，再尝试设施建造。
- 结果：入口验证通过，storage 仍为空，runtime errors 为 0；但最终关键格仍为 `frame`，未升级为 `mining` / `power` / `turret` / `repair`。
- 代表性记录：
  - `targetGridKey=11,-6`
  - `powerKey=2,0`
  - `turretKey=3,0`
  - `repairKey=4,0`
  - 最终 resources：`metal=11`、`ore=63`、`gas=37`、`plasma=8`、`powerAvailable=6`
  - 最终 `harvestingCount=0`

## PM 结论

**#6 仍未通过，继续阻塞 #7。**

本轮可以关闭旧的「playtest 入口自动结算 / storage 污染」疑点：全新临时 profile 首帧已默认暂停、非结算态、storage 为空。但首局 P0 建设链仍没有取得有效通过证据，采矿收益、资源不足提示、维修站建成与反馈均未闭环。

## 残留风险与下一步

- 风险：真实玩家路径或 CDP 可见鼠标路径下，建造选择 / 网格点击仍可能丢失或误命中；需要玩法 / 系统工程继续排查设施选择与建造输入可验证性。
- 风险：#5 已证明单独采矿收益可归因，但 #6 首局建设链仍不能证明玩家能从空档自然走到采矿、资源规划和维修。
- 下一步：#7 自然交火 / 防御 / 维修 / 战后复盘继续保持阻塞；应先由执行成员修复或提供可重复的真实输入建造路径，再由 PM 复测 #6。

## 2026-06-10 04:12 再次复跑（v1.0.0 #6）

### 04:12 基本信息

- 复测时间：2026-06-10 04:12-04:14 UTC+8
- 页面：`Game/index.html?playtest=1`
- 浏览器：可见 Chrome + CDP，独立临时 profile `agentgame-v100-p0-retest6-20260610-041256-9724`
- 新证据 JSON：`Workspaces/pm-qa-coordinator/v1.0.0-p0-build-resource-repair-retest-20260610-041256.json`
- 禁止项确认：未修改 `Game/`；未调用 `buildAt`；未直接修改 `state`、resources、storage 或 body amount；按钮与 canvas 均通过 CDP 鼠标事件点击，只读 hook / 只读状态采样用于记录。

### 04:12 已读取依据

- `Workspaces/pm-qa-coordinator/v1.0.0-release-candidate-kanban.md` #6 / #7
- `Workspaces/pm-qa-coordinator/v1.0.0-p0-build-plan-fix.md`
- `Game/game.js` 中 `__gameTest.playtest.getMiningBuildPlan()`、playtest 入口默认暂停、采矿引导 / resourceGuide / playtest 只读 hook

### 复测结果

| 项目 | 结果 |
| --- | --- |
| 入口默认暂停 | 通过：首帧 `entryPaused=true`，按钮文案为「继续」。 |
| 非结算入口 | 通过：`entryNotSettlement=true`，未进入本局结算，`objective.failed=false`。 |
| 全新 storage | 通过：`initialStorageEmpty=true`，`save.hasValue=false`，`corruptRaw.hasValue=false`。 |
| runtime errors | 通过：初始与最终均为 `0`。 |
| frame -> mining 计划执行 | 部分通过：按 `getMiningBuildPlan().currentStep` 重读并点击 `2,0 -> 3,0 -> 4,0 -> 5,0 -> mining@5,0`，采矿站建成，`miningBuilt=true`。 |
| 采矿收益 | 部分通过：过程中出现 `harvestingCount=1`，statusAlerts 显示「采矿进行中：等离子（行星-2）」，resourceGuide 显示目标 `行星-2 / 等离子` 剩余量从 `1177` 降至 `1145`；对应采样资源从 `metal=49, ore=35, gas=40, plasma=40` 记录到 JSON。 |
| 采矿稳定性 | 未通过：最终 `harvestingCountFinal=0`，矿站变为 out-of-range，statusAlerts 显示「采矿站待命：距『行星-2』还差 137，请进入彩色外环」与「采矿站未覆盖资源」。 |
| power / turret | 部分通过：`power.ok=true`、`turret.ok=true`，最终 `powerUsed=9`、`powerAvailable=16`。 |
| repair | 未通过：`repairBuilt=false`，最终 repair summary 为 `repair_missing`，`repairerCount=0`，`damagedCellCount=1`，`uncoveredDamagedCount=1`。 |
| 资源与反馈 | 未通过：最终 `metal=16, ore=24, gas=40, plasma=40`，仍选中「炮塔」且提示下一座还缺金属，维修站反馈未覆盖。 |

### 04:12 PM 结论

**#6 仍未通过，继续阻塞 #7。**

本轮关闭了上一轮「无法按 currentStep 建成采矿站」的部分疑点：在更宽视口下，CDP 鼠标点击命中 canvas，且 `getMiningBuildPlan()` 的 `frame -> mining` 计划可以推进到采矿站建成，并短暂取得 `harvestingCount=1` 与目标剩余量下降证据。但首局 P0 闭环仍失败：采矿不稳定并回到 out-of-range，维修站未建成，受损对象存在但 repair summary 为 `repair_missing`，资源规划与维修反馈未闭环。

### 04:12 残留风险与下一步

- 证据缺口：JSON 的 `targetAmounts.amount` 为 `null`，原因是本轮采样器读取了错误对象；本报告使用 resourceGuide 的「剩余 1177 -> 1145」作为 body amount 旁证，不把它包装成底层 body 字段采样。
- 产品风险：采矿收益曾短暂成立，但玩家路径仍可能在后续补设施时把矿站带出覆盖范围，导致“建成采矿站”不等于“可稳定用采矿收益支持下一步”。
- 下一步：#7 继续阻塞；建议执行成员优先处理首局采矿站覆盖稳定性、设施补建顺序与 repair 候选格选择，再由 PM 用同一证据路径复测。
