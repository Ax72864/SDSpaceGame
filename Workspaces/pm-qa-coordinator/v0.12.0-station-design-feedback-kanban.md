# v0.12.0-station-design-feedback PM 看板

## 本轮目标

版本名：`v0.12.0-station-design-feedback`。

本轮目标是把现有建造、资源、移动、战斗、研发、星系图和 Meta 成长从“功能可用”推进到“玩家能理解自己的空间站设计是否合理”。重点是空间站设计反馈与建造可读性：玩家打开建造面板、选择设施或检查右侧状态时，应能看懂设施角色、资源/电力短板、采矿范围、武器/护盾/维修状态和关键风险提示。

本版本不新增建筑类型，不修改存档，不修改核心玩法规则，不替玩家自动修复空间站。

## 背景

- 制作人方向文档：`Workspaces/game-producer-lead-designer/v0.12.0-station-design-feedback-direction.md`。
- 上游指定 commit：`8dd5734`。
- v0.11.0 已接受为“开发完成 / 自动与 headless 浏览器回归通过”，但真实人工发布验收仍未完成。
- v0.12.0 可以继续开发，不把 v0.11.0 人工发布验收作为启动阻塞；但 v0.12.0 是否进入发布准备，仍需 #10 制作人最终决定。

## 制作人方向摘要

- 建造入口从按钮列表升级为可比较的设计选择，至少能显示设施角色、成本、电力、可负担状态和已建数量。
- 放置/选择反馈需要解释资源不足、未连接核心、电力压力、采矿范围、武器/导弹边界、护盾能力和维修覆盖。
- 右侧状态或更多状态中增加空间站设计健康摘要，用 4-6 个短项总结电力、采矿、推进、火力、防御和维修短板。
- 低噪声问题提示只在关键风险出现时提醒，不把普通轻微低效刷成持续红字。
- 增加只读测试/验收钩子，输出 build palette、design health、selected cell diagnostics、resource reachability、power margin 等验证信息。
- 所有诊断只解释现有系统，不改变数值规则、存档结构、RNG、任务/事件注册表或战斗判定。

## 风险定级

- 风险等级：中风险。
- 中风险来源：UI 信息架构、诊断准确性、提示优先级、只读测试钩子字段、文案与实际规则一致性。
- 风险控制：默认只做展示、只读诊断、文案和测试输出；任何触及数值公式、资源/电力结算、战斗/护盾/维修判定、存档结构或 RNG 的行为必须升级为高风险并暂停执行。
- 高风险升级触发：修改 `SAVE_KEY`、meta schema、资源生产/消耗公式、电力结算公式、战斗命中/射线/导弹/护盾/装甲/维修判定、敌人/任务/星系图推进或 RNG。

## 严格范围

### 允许触碰

- 本看板创建阶段：仅允许新增/修改 `Workspaces/pm-qa-coordinator/v0.12.0-station-design-feedback-kanban.md`。
- 后续实现阶段在任务批准后仅允许触碰：
  - `Game/game.js` 中建造面板、设施诊断、右侧状态、低噪声提示、只读测试钩子相关区域。
  - `Game/index.html` 中建造面板、设施诊断、右侧状态、测试显示入口相关 DOM。
  - `Game/style.css` 中建造面板、诊断摘要、右侧状态和提示样式相关区域。
- 审核/回归阶段允许在本看板记录结论、问题、验证证据和残留风险。

### 禁止触碰

- 禁止修改 `42.md`。
- 禁止修改 `SAVE_KEY`、meta schema、存档结构和持久化迁移。
- 禁止修改 RNG、objective 注册表、encounter 注册表、敌人/任务/星系图推进规则。
- 禁止修改 `Releases/`、`Docs/`、`.cursor/`、旧看板、旧数值方案、方向文档和其他无关工作区文件。
- 禁止新增建筑类型，禁止修改设施数值规则，禁止改核心玩法判定。
- 禁止删除文件。
- 禁止 stage/commit dirty `42.md`。
- 禁止 `git reset`、`git checkout` 覆盖、`git clean`、`git stash`、force push 或改写已推送历史。

## 发布默认策略

默认不发布、不打 tag、不归档 `Releases/`。只有 #10 制作人明确批准“进入发布准备”后，才允许按发布流程创建 tag、构建归档和版本说明。

## 状态总览

### 待办

暂无待办任务。

### 进行中

- #10. 制作人最终决定 / 是否进入发布准备

### 待审核

暂无待审核任务。

### 完成

- #1. PM 看板创建（完成标准：本看板包含目标、背景、制作人方向摘要、风险定级、严格范围、状态总览、10 项任务、45 条以上 QA 回归点、默认不发布策略和自检记录；仅新增本看板文件）。
- #2. UI/信息架构方案（文档门槛通过：覆盖建造卡片字段、设施角色、健康摘要、低噪声提示、小屏兼容、DOM/CSS 落地建议和 52 条 UI 回归点；未提出改规则、存档或数值）。
- #3. 主程技术边界方案（文档门槛通过：覆盖诊断 helper、只读测试钩子、UI 接入点、缓存/性能、禁止改动清单、T1-T4 审查节点和回滚；明确不改 `SAVE_KEY`、`TYPES`、`getBuildCost`、资源/电力/战斗/建造判定）。
- #4. 数值/文案表（文档门槛通过：覆盖 13 种设施角色、短用途、诊断阈值、低噪声提示、Meta/协议显示边界、术语禁词和 30 条文案回归点；阈值仅用于展示分级，不改变公式）。
- #5. T1 建造面板设施卡可读性（PM 节点 A 回归通过：`11b444b`；自动检查、VM/DOM 字段、只读钩子、Meta/协议折扣成本和关键不变量通过；S2/S3 已记录）。
- #6. T2 设施选择/放置诊断反馈（PM 节点 B 回归通过：T2 `feb6e37`，S1 修复 `b44cb6a`；专项复审允许进入 PM 节点 B，无 S0/S1；自动检查、VM/DOM 诊断覆盖和只读不变量通过；S2/S3 已记录）。
- #7. T3 空间站设计健康摘要（PM 节点 C 回归通过，T3 提交 `7237305`；专项审核通过且允许 PM 节点 C，无 S0/S1；自动检查、VM/DOM/headless 沙箱、只读钩子和 snapshot design health 通过；S2/S3 已记录并作为 #8 前置清单）。
- #8. T4 低噪声问题提示 + 只读 playtest/build diagnostics 钩子（实现提交 `d5f611f`；全量专项审核通过，允许进入 #9；无 S0/S1）。
- #9. 专项审核 + PM 回归（PM 回归通过；自动检查、Chrome headless/CDP、只读钩子字段、低噪声提示、旧路径不回潮和性能粗测通过；真实浏览器人工体验与小屏触控留给 #10 制作人最终决定）。

### 阻塞

暂无阻塞任务。

## 任务清单

### #1. PM 看板创建

- 状态：完成。
- 负责人：`/pm-qa-coordinator`。
- 目标：创建 `Workspaces/pm-qa-coordinator/v0.12.0-station-design-feedback-kanban.md`，作为 v0.12.0 空间站设计反馈版本的 PM 主记录。
- 优先级：P0。
- 依赖：制作人方向文档已提交，指定 commit `8dd5734`。
- 允许修改范围：仅本看板文件。
- 禁止修改范围：`Game/`、`Docs/`、`.cursor/`、`42.md`、`Releases/`、其他工作区文件、旧看板、旧数值方案和方向文档。
- 验收标准：章节齐全；任务不少于 10 项；每项包含负责人、目标、优先级、依赖、允许修改范围、禁止修改范围、验收标准、风险；QA 回归点不少于 45 条；明确默认不发布；提交时只包含本看板文件。
- 风险：若误提交 dirty `42.md` 或旧看板会污染用户资料和历史版本记录；提交前必须检查 staged 范围。

### #2. UI/信息架构方案

- 状态：完成（PM 文档门槛通过）。
- 负责人：`/ui-artist`，PM 复核。
- 目标：定义建造卡片、设施角色、健康摘要和提示优先级的信息结构，让玩家能快速比较设施用途和当前问题。
- 优先级：P0。
- 依赖：#1 完成；制作人方向不变。
- 允许修改范围：可新增/修改 UI 方案记录到 UI 工作区；后续实现仅限 `Game/index.html`、`Game/style.css`、`Game/game.js` 中建造面板、设施诊断、右侧状态、提示相关区域。
- 禁止修改范围：禁止改 `42.md`、`SAVE_KEY`、meta schema、RNG、objective/encounter 注册表、核心数值规则、`Releases/`、`Docs/`、`.cursor/`、旧看板。
- 验收标准：建造卡片字段包含角色、成本、电力、可负担状态、已建数量和推荐/风险短句；设施角色至少覆盖采矿、供电、推进、武器、防御、维修、支撑；健康摘要控制在 4-6 个短项；提示优先级区分关键风险和普通低效。
- PM 门槛结论：通过。`Workspaces/ui-artist/v0.12.0-station-design-feedback-ui-plan.md` 已覆盖建造面板卡片结构、角色标签、可负担状态、放置/选择反馈、健康摘要 6 类短项、低噪声提示优先级、小屏兼容、DOM/CSS 建议和 UI 回归点；未发现越界到 `Game/`、数值规则、存档或 `42.md` 的要求。
- 风险：信息过多会降低可读性；提示过强会打断玩家；文案若暗示自动修复会超出版本范围。

### #3. 主程技术边界方案

- 状态：完成（PM 文档门槛通过）。
- 负责人：`/lead-game-programmer`。
- 目标：确定诊断 helper、只读测试钩子和实现边界，确保所有输出只解释现有状态，不改数值规则。
- 优先级：P0。
- 依赖：#1 完成；#2 初版信息结构可作为输入。
- 允许修改范围：可新增/修改主程工作区技术方案；后续实现仅限 `Game/game.js` 中诊断 helper、只读测试钩子、建造面板/右侧状态读取逻辑。
- 禁止修改范围：禁止修改 `SAVE_KEY`、meta schema、RNG、objective/encounter 注册表、资源/电力公式、战斗/护盾/维修判定、敌人/任务推进、`42.md`、`Releases/`、`Docs/`、`.cursor/`。
- 验收标准：列出诊断数据来源、字段名、只读约束、调用时机、不会写存档的证明点；明确 `__gameTest` 或等价测试入口读取不会改变资源、敌人、任务进度和随机结果。
- PM 门槛结论：通过。`Workspaces/lead-game-programmer/v0.12.0-station-design-feedback-technical-plan.md` 已列出诊断 helper schema、只读不变量、UI 接入点、缓存/性能边界、`__gameTest.playtest` 扩展字段、禁止改动清单、T1-T4 节点审查条件和回滚方式；文档明确禁止修改 `SAVE_KEY`、meta schema、`TYPES`、`getBuildCost`、资源/电力公式、建造合法性和战斗判定。
- 风险：helper 若复用不当可能改变运行时状态；测试钩子字段若过窄会无法支撑 QA 回归。

### #4. 数值/文案表

- 状态：完成（PM 文档门槛通过）。
- 负责人：`/systems-numerics-designer`，PM 复核。
- 目标：整理设施角色、诊断阈值、提示文案和误导风险，保证文案解释与现有规则一致。
- 优先级：P0。
- 依赖：#2 信息架构；#3 技术边界。
- 允许修改范围：可新增/修改数值/文案方案记录；后续实现仅限 `Game/game.js` 中展示文案、诊断阈值读取、建造面板文案映射。
- 禁止修改范围：禁止调整设施成本、产出、耗电、资源生成、敌人、任务、RNG、存档结构、`42.md`、`Releases/`、`Docs/`、`.cursor/`。
- 验收标准：设施角色表覆盖现有建筑；诊断阈值只用于展示分级，不改变规则；提示文案能指出具体原因，如“采矿站未覆盖资源”“武器断电”“敌袭前无防御”；Meta/协议加成显示不误导为新规则。
- PM 门槛结论：通过。`Workspaces/pm-qa-coordinator/v0.12.0-station-design-feedback-copy-and-thresholds.md` 已覆盖 13 种设施角色、短用途、常见不工作原因、健康摘要 6 类阈值、低噪声提示优先级、Meta/协议加成文案边界、术语禁词和 30 条文案回归点；明确所有阈值只用于展示分级，不改变底层结算或公式。
- 风险：阈值过严会造成噪声，过松会漏掉关键风险；文案若与真实公式不一致会损害信任。

### #5. T1 建造面板设施卡可读性

- 状态：完成（PM 节点 A 回归通过，T1 提交 `11b444b`）。
- 负责人：`/senior-gameplay-engineer`。
- 目标：让建造面板设施卡清楚显示设施角色、成本、电力影响、可负担状态、已建数量和短推荐理由。
- 优先级：P0。
- 依赖：#2、#3、#4 均已通过 PM 文档门槛。
- 启动条件：T1 只允许修改建造面板 UI、建造面板展示文案、建造面板相关只读诊断和只读测试输出；只允许读取现有规则结果。禁止修改 `TYPES` 数值、`getBuildCost` 公式、建造合法性、资源公式、`SAVE_KEY`、meta schema、RNG、objective/encounter 注册表、战斗/护盾/维修判定。
- 允许修改范围：`Game/game.js`、`Game/index.html`、`Game/style.css` 中建造面板卡片渲染、字段读取、样式和相关只读测试输出。
- 禁止修改范围：禁止新增建筑类型，禁止修改设施成本/产出/耗电规则，禁止改存档、RNG、objective/encounter 注册表、`42.md`、`Releases/`、`Docs/`、`.cursor/`。
- 验收标准：空存档打开建造面板能区分采矿、供电、推进、武器、防御、维修设施；资源不足时尝试建造前能看到原因；Meta/协议影响成本或初始能力时能看到变化后的展示结果；headless DOM 可读取卡片关键字段。
- PM 节点 A 结论：通过，允许进入 #6。专项审核已通过且无 S0/S1；PM 复测未发现阻塞接受问题。`node --check Game/game.js`、`git diff --check` 均通过；T1 diff 仅包含 `Game/game.js`、`Game/style.css`，本次 PM 提交只允许记录本看板。
- PM 节点 A 验证摘要：VM/DOM 沙箱读取到 12 个可建设施按钮（`FACILITY_ORDER` 全量，不含核心；另有“指针/航行”按钮），设施卡 `role`、成本、电力、可负担状态、已建数量、用途、状态和 `data-*` 完整；资源置零后所有可建设施进入不可负担状态并显示缺口；`__gameTest.playtest.getBuildPaletteDiagnostics()` 前后资源、敌人、NPC、任务、seed 和 time 快照一致；palette 成本逐项等于 `getBuildCost`；`SAVE_KEY` 为 `star-ring-station-meta-v1`、`TYPES` 仍为 13 种设施、`getBuildCost` 未被 T1 修改。
- PM 节点 A 性能粗测：VM/DOM 中连续 250 次 `updateBuildButtons()` 合计约 89.7ms，约 0.36ms/次，未见明显超预算；该结果不是真实浏览器帧耗时，只作为粗略风险观察。
- PM 节点 A 残留问题：S2-01 `updateBuildButtons()` 每次重写设施卡 `innerHTML`，后续设施更多或刷新频率升高时可能形成 DOM 性能压力，#6/#8 阶段继续观察；S2-02 推荐/警告样式依赖 `tipText` 文案前缀“推荐/警告”，文案改动可能破坏 class，后续应改为显式等级字段；S2-03 少量设施提示文案未命中时会回落为空或基础描述，需在后续文案回归补齐；S3-01 本次用户检查项写“建造面板 11 个按钮”，当前实现和既有 `FACILITY_ORDER` 为 12 个可建设施按钮，PM 按“覆盖全部现有可建设施、不新增建筑类型”通过，但需在后续验收口径中确认是否排除 `frame`。
- 风险：卡片密度过高影响移动端/小窗口可读性；折扣显示若与实际消耗不一致会造成误导。

### #6. T2 设施选择/放置诊断反馈

- 状态：完成（PM 节点 B 回归通过，T2 提交 `feb6e37`，S1 修复 `b44cb6a`）。
- 负责人：`/senior-gameplay-engineer`。
- 目标：准备建造或选中设施时，显示资源、连接、电力、采矿范围、武器/护盾/维修状态相关诊断。
- 优先级：P0。
- 依赖：#3、#4、#5。
- 允许修改范围：`Game/game.js`、`Game/index.html`、`Game/style.css` 中选中格、建造预览、设施详情、诊断面板和只读测试钩子相关区域。
- 禁止修改范围：禁止修改建造合法性规则、连接规则、采矿判定、电力结算、战斗射线、护盾/维修判定、存档结构、`42.md`、`Releases/`、`Docs/`、`.cursor/`。
- 验收标准：资源不足、未连接核心、断电、采矿站远离资源外环、武器/导弹边界、护盾状态、维修覆盖均有可读反馈；按钮无反应路径必须有解释；测试钩子能读取 selected cell diagnostics。
- PM 节点 B 结论：通过，允许进入 #7。专项复审已明确允许 PM 节点 B；当前无 S0/S1。`node --check Game/game.js`、`git diff --check` 均通过；本次 PM 只提交本看板，不提交 dirty `42.md`、`Game/`、`Docs/`、`Releases/` 或其他工作区文件。
- PM 节点 B 验证摘要：VM/DOM/headless 沙箱读取 `__gameTest.playtest.getSelectedCellDiagnostics()` 覆盖 `mining`、`turret`、`shield`、`repair`、`power`；`getPlacementDiagnostics()` 覆盖 `mining`、`turret`、`power`。调用诊断钩子前后 `priorityTarget`、`resources`、`cells`、`localStorage` 快照一致；`buildHint`、`resourceGuide`、`statusAlerts` 未出现 `undefined`。
- PM 节点 B 静态边界：T2+S1 diff 集中在设施诊断文案/状态、placement preview、selected diagnostics、resource guide/status alert 接入、只读 playtest 钩子和 S1 只读索敌修复；未发现 `SAVE_KEY`、meta schema、`TYPES` 注册表、`getBuildCost`、资源/电力结算、建造合法性、objective/encounter 注册表、RNG 或存档写入越界修改。
- PM 节点 B 残留问题：S2-04 同格状态变化后 `resourceGuide` 可能短暂复用 stale `placementPreview`，当前专项复审判定不阻塞节点 B，后续 #7/#8 继续观察并在高频 HUD 刷新路径中优先复测；S3-02 本节点未覆盖真实浏览器人工体验、移动端触控可读性和真实帧耗时，不得据此宣称发布就绪。
- 风险：诊断若提前调用建造逻辑可能引入副作用；范围提示若过度精确但不可信会误导玩家。

### #7. T3 空间站设计健康摘要

- 状态：完成（PM 节点 C 回归通过，T3 提交 `7237305`）。
- 负责人：`/senior-gameplay-engineer`，`/ui-artist` 协作。
- 目标：在右侧状态或更多状态中加入空间站设计健康摘要，帮助玩家看到关键 1-2 个短板。
- 优先级：P1。
- 依赖：#2、#3、#4、#6。
- 允许修改范围：`Game/game.js`、`Game/index.html`、`Game/style.css` 中右侧状态、健康摘要 DOM/样式、只读 design health 输出。
- 禁止修改范围：禁止修改推进、采矿、战斗、防御、维修、资源和电力规则；禁止改 `SAVE_KEY`、meta schema、RNG、`42.md`、`Releases/`、`Docs/`、`.cursor/`。
- 验收标准：摘要覆盖电力余量、采矿能力、推进能力、武器火力、防御/护盾/装甲、维修能力中的 4-6 项；战斗前或跃迁前能指出明显短板；文案只做诊断和建议，不承诺自动优化。
- PM 节点 C 结论：通过，允许进入 #8。专项审核已通过且无 S0/S1；PM 复测未发现阻塞接受问题。`node --check Game/game.js`、`git diff --check` 均通过；T3 diff 仅包含 `Game/game.js`、`Game/index.html`、`Game/style.css`，本次 PM 提交只允许记录本看板，不提交 dirty `42.md`。
- PM 节点 C 验证摘要：VM/DOM/headless 沙箱读取 `__gameTest.playtest.getStationDesignHealth()` 覆盖 `power`、`mining`、`thrust`、`firepower`、`defense`、`repair` 6 类；右侧更多状态 DOM 中 `design-health-item` 为 6 项；空存档摘要显示采矿、推进、火力、防御短板；添加供电、推进、炮塔、护盾、维修设施后状态合理改善；移除炮塔/护盾并制造电力赤字后火力、防御、电力状态合理恶化；只读钩子读取前后资源、敌人、任务、seed、time、cells 和 storage 快照一致；`__gameTest.playtest.snapshot().design.health` 存在。
- PM 节点 C 静态边界：T3 diff 集中在健康摘要常量、`getStationDesignHealth()` 只读计算、右侧状态 DOM 接入、样式和 playtest snapshot 字段；禁止项扫描未发现 `SAVE_KEY`、meta schema、`getBuildCost`、资源/电力结算、objective/encounter 注册表、RNG 或存档写入越界修改。
- PM 节点 C 性能粗测：VM/DOM 沙箱连续 1000 次 `getStationDesignHealth()` 合计约 18.498ms，约 0.0185ms/次；未见明显性能风险。该结果不是真实浏览器帧耗时，不能替代后续人工体验和浏览器性能观察。
- PM 节点 C 残留问题：S2-05 技术方案要求的 0.18s 采样缓存尚未落地，当前每次 HUD 更新直接计算健康摘要；S2-06 playtest 输出 schema 与技术方案字段存在偏差，当前为 `snapshot().design.health` 和 `getStationDesignHealth()`，后续 #8 需统一验收字段；S2-07 缺少 `getResourceReachability()` 与 `getPowerMargin()` 只读钩子，#8 必须补齐；S3-03 本节点未覆盖真实浏览器人工体验、移动端触控可读性和真实帧耗时，不得据此宣称发布就绪。
- 风险：摘要过长会遮蔽核心状态；摘要过短会漏掉关键问题；战斗前提示若时机不当会增加压力。

### #8. T4 低噪声问题提示 + 只读 playtest/build diagnostics 钩子

- 状态：完成（T4 提交 `d5f611f`；全量专项审核通过，允许进入 #9）。
- 负责人：`/senior-systems-engineer`，PM 提供验收字段。
- 目标：增加关键风险提示和只读 playtest/build diagnostics 输出，支持自动检查、headless DOM 和后续人工体验记录。
- 优先级：P1。
- 依赖：#3、#4、#7。
- 允许修改范围：`Game/game.js`、`Game/index.html`、`Game/style.css` 中提示显示、只读测试钩子、debug snapshot 或现有测试入口相关区域。
- 禁止修改范围：禁止写存档，禁止改变资源、敌人、任务进度、随机结果、核心规则、`SAVE_KEY`、meta schema、RNG、objective/encounter 注册表、`42.md`、`Releases/`、`Docs/`、`.cursor/`。
- 验收标准：只在关键风险出现时提示，如采矿站全不可达、武器/导弹断电、敌袭前无基本防御、大量核心能力断电、跃迁前健康摘要存在明显短板；测试钩子能读取 build palette、design health、selected cell diagnostics、resource reachability、power margin；读取钩子不改变游戏状态。
- T4 前置清单：补齐 `__gameTest.playtest.getResourceReachability()`，输出采矿站与资源覆盖状态，读取不得写存档或改变资源/任务/RNG；补齐 `__gameTest.playtest.getPowerMargin()`，输出电力产出、消耗、余量、断电设施和核心能力断电影响；确认 `snapshot().design.health` 与 `getStationDesignHealth()` 的 schema 是否作为正式验收口径，若偏离 #3 技术方案需同步记录；处理或解释 0.18s 健康摘要采样缓存，避免高频 HUD 刷新路径重复计算；低噪声提示优先复用 `reasonKey`、selected diagnostics 和 placement diagnostics，不新增规则或数值判定。
- 完成结论：通过。全量专项审核已确认无 S0/S1，允许进入 #9；T4 补齐 `getResourceReachability()`、`getPowerMargin()`、snapshot design 字段和低噪声 `statusAlerts` 接入。
- 风险：提示触发过频会噪声化；测试钩子若字段未冻结会影响回归脚本稳定性。

### #9. 专项审核 + PM 回归

- 状态：完成（PM 回归通过，允许 #10 制作人最终决定进入进行中）。
- 负责人：`/pm-qa-coordinator` 统筹，`/lead-game-programmer`、`/ui-artist`、`/systems-numerics-designer` 分别专项审核。
- 目标：确认实现满足范围、诊断准确、UI 可读、文案不误导，并完成 PM 自动/headless/人工记录。
- 优先级：P0。
- 依赖：#5、#6、#7、#8 完成并自检。
- 允许修改范围：本看板记录审核结论、回归结果、缺陷和残留风险；缺陷修复仅限对应任务允许范围。
- 禁止修改范围：禁止借回归名义修改 `42.md`、`SAVE_KEY`、meta schema、RNG、核心规则、`Releases/`、`Docs/`、`.cursor/`、无关工作区文件。
- 验收标准：专项审核无 S0/S1/S2 阻断；PM 回归覆盖本看板 QA 回归点；自动检查、headless DOM、真实浏览器人工未覆盖路径分别记录；缺陷修复后按原复现步骤复测。
- PM #9 结论：通过。全量专项审核已通过且无 S0/S1；本次 PM 复测未发现 S2 阻断，允许进入 #10。真实浏览器人工体验、移动端/小屏触控可读性和完整手动最小采矿链仍未覆盖，不宣称发布就绪。
- PM #9 自动检查：`node --check Game/game.js` 通过；`git diff --check` 通过。检查前 `git status --short` 仅显示用户 dirty `42.md`，本次 PM 不提交。
- PM #9 Chrome headless/CDP：build palette 12 个设施，`id/name/role/cost/powerImpact/affordable/count` 字段齐全；selected diagnostics 覆盖 `core/power/mining/turret/shield/repair`，武器、护盾、维修和范围字段按设施类型出现；placement diagnostics 覆盖 `facility/affordability/placement/powerForecast/rangeHint/warnings`；design health 覆盖 `power/mining/thrust/firepower/defense/repair` 6 类，`shortItems` 为 6；resource reachability 和 power margin 可读；`snapshot().design.health/resource/reachability/power/criticalIssues` 字段齐全。
- PM #9 只读钩子结论：连续读取 build palette、selected、placement、station design health、resource reachability、power margin 和 snapshot 前后，资源、敌人/NPC 数量、任务、seed、time、priorityTarget、station cells 与 `localStorage` 签名一致；诊断钩子只读通过。
- PM #9 低噪声提示：多风险场景下 `criticalIssues` 包含敌袭无防御、多处断电、采矿无目标、武器断电、护盾断电等，DOM 中 `.alert-design-issue` 最多 2 条，优先显示敌袭无防御与多处断电；隔离冷却场景验证为首次出现 1 条、风险消失后清空、8 秒冷却内不重复、冷却后恢复显示。critical issues 与 `statusAlerts` 对齐通过。
- PM #9 旧路径不回潮：v0.5 研发树入口、`unlockGlobalResearch` 和研发设施角色仍可读；v0.9 galaxy jump state machine 与 galaxy visual self check 通过，HUD galaxy snapshot 可读；v0.10 Meta S2 regression 和 panel mutual exclusion 通过；v0.11 playtest `runAllSelfChecks()` 通过，snapshot version 保持 `v0.11.0-playtest-readiness` 且 runtimeErrors 字段存在。
- PM #9 DOM 覆盖：Chrome headless 中建造卡 12 个、角色标签 12 个、design health DOM 6 项、selected diagnostics 容器可显示且 HTML 非空、status alerts 可读，未发现 `undefined`。本项是 headless/CDP 检查，不等同真实人工体验或小屏触控验收。
- PM #9 性能粗测：Chrome headless 中强制跨采样间隔读取 `getStationDesignHealth()` 1000 次约 33.2ms，约 0.0332ms/次；`updateHud()` 120 次约 32.2ms，约 0.268ms/次，覆盖等价的 build button/design health/status alert 刷新路径。该结果不是人工体验帧耗时。
- S2/S3 复测结论：S2-01 建造按钮仍存在 `innerHTML` 重写路径，但当前 headless 粗测未见明显性能风险，继续作为后续优化项；S2-02 tip class 依赖文案前缀仍存在，未阻塞 #9，后续建议改为显式等级字段；S2-03 本次字段与 DOM 未见 `undefined` 或缺字段，少量未命中文案风险降为观察；S2-04 placement/selected/playtest 读取签名不变，未复现 stale 造成的阻塞；S2-05 0.18s 健康摘要采样缓存已落地并可读；S2-06 snapshot design 与验收字段齐全；S2-07 `getResourceReachability()` 与 `getPowerMargin()` 已补齐；提示并发排序和最多 2 条通过。S3 真实人工体验、移动端触控可读性、真实帧耗时仍未覆盖，交 #10 判断。
- 风险：真实浏览器人工体验若未覆盖，不得宣称发布就绪；只通过 headless 不能替代手感和可读性判断。

### #10. 制作人最终决定 / 是否进入发布准备

- 状态：进行中。
- 负责人：`/game-producer-lead-designer`，PM 提供证据包。
- 目标：根据专项审核、PM 回归和体验证据，决定 v0.12.0 是否进入发布准备，或继续打磨。
- 优先级：P0。
- 依赖：#9 完成。
- 允许修改范围：制作人可在对应工作区记录最终结论；若批准发布，再由后续发布流程处理 `Releases/`、tag 和发布说明。
- 禁止修改范围：未批准前禁止发布、打 tag、归档 `Releases/`；禁止修改 `42.md`、核心规则、存档结构和无关文件。
- 验收标准：制作人结论明确为“进入发布准备”或“继续打磨”；若进入发布准备，必须列出发布前剩余动作；若继续打磨，必须列出阻塞原因和下一轮优先级。
- PM 交接给 #10：#9 自动/headless/CDP 回归通过，无 S0/S1/S2 阻断；发布判断仍缺真实浏览器人工体验、小屏触控可读性和真实帧耗时证据。建议制作人先做至少一轮空存档手动最小采矿链、资源不足/断电/采矿远离外环、敌袭前健康摘要和提示噪声体验，再决定进入发布准备或继续打磨。
- 风险：若缺少真实浏览器人工证据，发布判断应保守；若诊断未能让玩家说出空间站短板，应回到 #2-#8 继续打磨。

## QA 回归点

### A. 空存档建造采矿链

- [ ] A01. 清空/重置运行后进入游戏，建造面板默认可打开且无控制台阻断错误。
- [ ] A02. 空存档建造面板能区分采矿、供电、推进、武器、防御、维修、支撑设施角色。
- [ ] A03. 采矿设施卡显示用途、成本、电力影响、可负担状态和已建数量。
- [ ] A04. 供电设施卡显示供电角色和对电力余量的影响。
- [ ] A05. 玩家按推荐或角色提示能完成最小采矿链，不需要猜测按钮含义。
- [ ] A06. 建造采矿链后，右侧状态或健康摘要能反映采矿能力从缺失变为存在。

### B. 资源不足与建造失败

- [ ] B01. 当前资源不足时，建造卡片在尝试前显示缺口或不可负担原因。
- [ ] B02. 资源不足时点击建造不会只表现为无响应，必须出现可读反馈。
- [ ] B03. 多资源同时不足时，提示能指出至少一个主缺口，不刷满屏重复红字。
- [ ] B04. 资源恢复到可负担后，卡片可负担状态刷新。
- [ ] B05. 折扣或加成影响成本时，不足提示基于实际展示成本，而不是原始成本。

### C. 断电与电力压力

- [ ] C01. 建造高耗电设施前，卡片或诊断能提示可能拉低电力余量。
- [ ] C02. 当前断电时，设施详情或健康摘要能说明关键能力受影响。
- [ ] C03. 大量设施断电时，低噪声提示只聚焦核心能力，如采矿、武器、防御、推进。
- [ ] C04. 补充供电后，断电诊断和健康摘要能刷新为较健康状态。
- [ ] C05. 电力产出、消耗、余量在只读 power margin 钩子中可读取。

### D. 远离资源外环与采矿范围

- [ ] D01. 准备建造采矿站时，系统能说明当前位置是否接近资源外环。
- [ ] D02. 采矿站远离资源外环时，提示覆盖不足或采不到资源。
- [ ] D03. 已存在采矿站但全部采不到资源时，低噪声提示能指出“采矿站未覆盖资源”或等价原因。
- [ ] D04. 靠近资源外环放置后，采矿诊断不再显示远离风险。
- [ ] D05. 只读 resource reachability 钩子能列出采矿站与资源覆盖状态。

### E. 武器/护盾/维修状态

- [ ] E01. 选中武器设施时，能看到可用状态、缺电状态或作用边界说明。
- [ ] E02. 选中导弹/远程攻击相关设施时，能看到方向、边界或可攻击状态说明。
- [ ] E03. 敌袭前没有武器或武器断电时，健康摘要能指出火力短板。
- [ ] E04. 护盾设施存在时，玩家能识别当前具备基础防护。
- [ ] E05. 护盾设施缺电或防护不足时，诊断不误导为完全安全。
- [ ] E06. 维修设施存在时，玩家能理解维修覆盖或维修能力状态。
- [ ] E07. 受损设施附近有/无维修覆盖时，诊断结果可区分。

### F. 建造折扣、Meta 与协议加成显示

- [ ] F01. 建造折扣存在时，卡片显示变化后的成本。
- [ ] F02. 折扣显示不改变实际扣费规则，只反映现有结果。
- [ ] F03. Meta 加成影响初始资源、成本或能力时，建造面板能观察到变化。
- [ ] F04. 协议加成影响成本、资源或能力时，建造面板/摘要能观察到变化。
- [ ] F05. 无 Meta/协议加成时，展示回到基线，不残留加成文案。
- [ ] F06. 加成文案不承诺新系统或未实现效果。

### G. v0.5/v0.9/v0.10/v0.11 不回潮

- [ ] G01. v0.5 研发系统入口、研发状态和既有测试钩子不因建造反馈改动失效。
- [ ] G02. v0.5 研发相关数值不被本轮诊断阈值修改。
- [ ] G03. v0.9 星系图路径选择、当前星系显示和跃迁流程不被右侧摘要破坏。
- [ ] G04. v0.9 objective/encounter 权重和 RNG 不被本轮测试钩子读取污染。
- [ ] G05. v0.10 Meta 成长读取和显示仍按既有 schema 工作，不新增迁移。
- [ ] G06. v0.10 已有 Meta 存档能正常加载，不因 v0.12 诊断字段报错。
- [ ] G07. v0.11 playtest readiness 相关测试入口仍可执行。
- [ ] G08. v0.11 headless 浏览器主路径不因 DOM 调整找不到关键按钮。
- [ ] G09. 旧的 objective/encounter 注册表内容和语义未变化。
- [ ] G10. `SAVE_KEY` 保持不变，旧存档兼容路径不新增强制字段。

### H. Headless DOM 与只读测试钩子

- [ ] H01. headless 环境能读取 build palette 中的设施角色、成本、电力、可负担状态和已建数量。
- [ ] H02. headless 环境能读取 design health 的电力、采矿、推进、火力、防御、维修摘要。
- [ ] H03. headless 环境能读取 selected cell diagnostics 的连接、资源、电力、范围和边界信息。
- [ ] H04. headless 环境读取测试钩子前后，资源数量不变化。
- [ ] H05. headless 环境读取测试钩子前后，敌人数量和任务进度不变化。
- [ ] H06. headless 环境读取测试钩子前后，随机结果不变化。
- [ ] H07. DOM 中建造卡片字段有稳定选择器或等价可读结构，支持 PM 回归脚本。
- [ ] H08. 控制台无新增 S0/S1 错误；警告若存在必须记录影响范围。

### I. 真实浏览器人工未覆盖记录

- [ ] I01. 真实浏览器人工体验需记录使用浏览器、分辨率、输入方式和时长。
- [ ] I02. 若未完成真实浏览器人工体验，#9 必须标记“人工体验未覆盖”，不得宣称发布就绪。
- [ ] I03. 人工体验需尝试从空存档完成最小采矿链，并记录是否能理解下一步。
- [ ] I04. 人工体验需故意制造资源不足、断电、采矿远离外环三类问题，并记录提示是否可读。
- [ ] I05. 人工体验需在敌袭或战斗前检查健康摘要是否指出武器/防御短板。
- [ ] I06. 人工体验需记录最噪的一条提示和最有帮助的一条提示，供制作人判断是否继续打磨。

## 回归记录模板

- 自动检查：PM 节点 A 已记录。`node --check Game/game.js` 通过；`git diff --check` 通过；`git status --short` 显示仅有用户 dirty `42.md`，本次 PM 不提交。
- Headless DOM：PM 节点 A 使用 VM/DOM 沙箱完成。建造面板设施卡字段和 `data-*` 完整；资源不足、设施数量、Meta/协议折扣成本与 `getBuildCost` 一致；只读钩子读取前后快照一致；`TYPES`、`getBuildCost`、`SAVE_KEY` 关键不变量未被 T1 修改。
- PM 节点 B 自动检查：`node --check Game/game.js` 通过；`git diff --check` 通过。
- PM 节点 B VM/DOM/headless：selected diagnostics 覆盖 `mining`、`turret`、`shield`、`repair`、`power`；placement diagnostics 覆盖 `mining`、`turret`、`power`；调用诊断钩子前后 `priorityTarget`、`resources`、`cells`、`localStorage` 不变；`buildHint`、`resourceGuide`、`statusAlerts` 无 `undefined`。
- PM 节点 B 静态确认：T2 `feb6e37` 与 S1 `b44cb6a` 未越界修改核心禁止项；S1 对索敌逻辑的改动是拆出只读候选读取，并保留正式 `selectTarget()` 对 stale `priorityTarget` 的清理行为。
- PM 节点 C 自动检查：`node --check Game/game.js` 通过；`git diff --check` 通过。
- PM 节点 C VM/DOM/headless：`getStationDesignHealth()` 覆盖电力、采矿、推进、火力、防御、维修 6 类；DOM `design-health-item` 为 6 项；空存档、添加设施、移除设施/电力赤字状态变化合理；读取健康摘要钩子前后资源、敌人、任务、seed、time、cells 和 storage 快照一致；`snapshot().design.health` 存在；性能粗测 1000 次约 18.498ms，约 0.0185ms/次。
- PM 节点 C 静态确认：T3 `7237305` 只改 `Game/game.js`、`Game/index.html`、`Game/style.css`；未发现 `SAVE_KEY`、meta schema、`getBuildCost`、资源/电力结算、objective/encounter 注册表、RNG、存档写入或禁止目录越界修改。
- 真实浏览器人工体验：PM 节点 A/B/C 均未覆盖；当前只做自动与 VM/DOM 回归，不宣称发布就绪。
- 未覆盖路径：真实浏览器帧耗时、移动端触控可读性、完整从空存档手动建造路径、#8 低噪声提示、`getResourceReachability()`、`getPowerMargin()` 尚未覆盖。
- 阻塞接受问题：PM 节点 A/B/C 未发现 S0/S1；已记录 S2-01 DOM 重写性能、S2-02 tip class 依赖文案前缀、S2-03 少量未命中文案、S2-04 同格状态变化后 `resourceGuide` 可能短暂复用 stale `placementPreview`、S2-05 缺 0.18s 采样缓存、S2-06 schema 与技术方案偏差、S2-07 缺 `getResourceReachability/getPowerMargin` 钩子；S3-01 “11/12 个按钮”验收口径待确认；S3-02 PM 节点 B 未覆盖真实浏览器人工体验；S3-03 PM 节点 C 未覆盖真实浏览器人工体验、移动端触控可读性和真实帧耗时。
- 缺陷复测结论：T1/T2/T3 专项审核已通过，PM 节点 B 针对 S1 只读索敌修复完成回归；`priorityTarget` 在诊断钩子读取前后不变，失败路径已关闭。PM 节点 C 未新增 S0/S1；上述 S2/S3 进入后续任务观察或修正。
- PM #9 自动检查：`node --check Game/game.js` 通过；`git diff --check` 通过。工作区仍有用户 dirty `42.md`，本次不提交。
- PM #9 Chrome headless/CDP：只读钩子字段齐全且读取前后状态签名一致；build palette 12 项、selected/placement/design health/resource reachability/power margin/snapshot design 字段齐全；DOM 中建造卡 12 个、设施角色 12 个、健康摘要 6 项、selected diagnostics 可显示、status alerts 可读；runtimeErrors 为 0。
- PM #9 低噪声提示：多 critical issues 场景下 status alerts 最多 2 条且按优先级显示；隔离冷却场景验证首次显示、风险消失清空、冷却内不重复、冷却后恢复。
- PM #9 旧路径不回潮：v0.5 research、v0.9 galaxyMap、v0.10 meta/settlement、v0.11 playtest snapshot/selfChecks 均通过。
- PM #9 性能粗测：Chrome headless 中 `getStationDesignHealth()` 1000 次约 33.2ms，约 0.0332ms/次；`updateHud()` 120 次约 32.2ms，约 0.268ms/次。该结果不是人工体验帧耗时。
- PM #9 残留未覆盖：真实浏览器人工体验、小屏/移动端触控可读性、真实帧耗时、完整手动从空存档完成最小采矿链未覆盖；已交 #10 制作人最终决定。

## 自检

- [x] 已包含本轮目标、背景、制作人方向摘要和风险定级。
- [x] 已写明严格范围：允许触碰 `Game/game.js`、`Game/index.html`、`Game/style.css` 的建造面板、设施诊断、右侧状态、测试钩子相关区域；禁止 `42.md`、`SAVE_KEY`、meta schema、RNG、objective/encounter 注册表、`Releases/`、`Docs/`、`.cursor/`、旧看板等无关文件。
- [x] 已包含待办 / 进行中 / 待审核 / 完成 / 阻塞状态总览。
- [x] 已包含 #1-#10 共 10 项任务。
- [x] 每项任务均包含负责人、目标、优先级、依赖、允许修改范围、禁止修改范围、验收标准和风险。
- [x] QA 回归点共 55 条，覆盖空存档建造采矿链、资源不足、断电、远离资源外环、武器/护盾/维修状态、建造折扣显示、Meta/协议加成、v0.5/v0.9/v0.10/v0.11 不回潮、headless DOM 和真实浏览器人工未覆盖记录。
- [x] 已明确默认不发布、不打 tag、不归档 `Releases/`，除非 #10 制作人另行批准。
- [x] 本次看板创建阶段只允许提交本看板文件，不提交 dirty `42.md`。
