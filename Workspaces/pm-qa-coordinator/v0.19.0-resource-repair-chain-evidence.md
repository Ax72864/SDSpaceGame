# v0.19.0-resource-repair-chain-evidence 可见浏览器证据

## 基本信息

- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 01:16 +08:00。
- 入口：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 浏览器：可见 Chrome，CDP 端口 `9393`。
- 临时 profile：`C:\Users\ylswd\AppData\Local\Temp\agentgame-v019-profile-20260610-011454-9393`。
- 输入方式：DOM build 按钮点击 + CDP `Input.dispatchMouseEvent` 物理鼠标事件；点击前用 UI 暂停稳定坐标，等待窗口用 UI 恢复运行。
- 原始 JSON：`Workspaces/pm-qa-coordinator/v0.19.0-resource-repair-chain-evidence-20260610-0118-raw.json`。
- 边界：未调用 `buildAt`，未直接修改 `window.__gameState`，未调用 `resetRun`，未清空或写入用户真实存档。

## Helper 与 Storage

- helper 可用：`getBuildPaletteDiagnostics`、`getPlacementDiagnostics`、`getGridScreenPoint`、`getGridInteractionDiagnostics`、`compareGridClickAlignment`、`getResourceReachability`、`snapshot`。
- storage signature：开始与结束均为 `localStorageLength=0`、`sessionStorageLength=0`、`localKeys=[]`、`sessionKeys=[]`。
- runtime errors：最终为 `0`；各关键步骤记录中也为 `0`。
- 视口：`1374x914`，DPR `2`。

## 建造链结果

| 步骤 | gridKey | 结果 | 资源变化摘要 | 点击对齐 |
| --- | --- | --- | --- | --- |
| `frame -> power` | `-1,-1` | 建成 `power` | 初始 `metal 160.59 / ore 65.63 / plasma 8.78`；`power` 后目标格为 `power` | `aligned=true`，`screenDelta.distance≈0` |
| `frame -> mining` | `-1,1` | 建成 `mining` | `mining` 前 `metal 122.59 / ore 57.63`；10 秒等待前 `metal 94.59 / ore 47.63` | `aligned=true`，`screenDelta.distance≈0` |
| `frame -> turret` | `-2,0` | 建成 `turret` | 45 秒补资源等待前 `metal 60.09 / ore 37.43 / plasma 9.03` | `aligned=true`，`screenDelta.distance≈0` |
| `frame -> repair` | `0,-2` | 建成 `repair` | `repair` 前 `metal 108.35 / plasma 10.16`；最终 `metal 56.35 / plasma 4.16` | `aligned=true`，`screenDelta.distance≈0` |

## Repair 结论

`repair` 本轮通过正常 UI 路径建成。证据显示建造前资源足够支付 `repair` 成本 `metal 44 / plasma 6`，先在 `0,-2` 扩出 `frame`，再将同一格替换为 `repair`；最终 `0,-2` 的 `facility=repair`，资源从 `metal 108.35 / plasma 10.16` 经 `frame` + `repair` 后到 `metal 56.35 / plasma 4.16`，与成本一致。

注意：建成后因当前仍选择 `repair`，HUD 继续显示下一座维修站的资源不足提示：`建造「维修站」资源不足：金属44 等离子6（核心会缓慢产金属）`，同时 toast 为 `维修站已建造。`。这不是本次建造失败，但提示混杂可作为后续 #5 的可读性观察点。

## Mining 结论

`mining` 建成但采矿收益未闭环。`mining` 位于 `-1,1`，建成后 `getResourceReachability` 显示 `minerCount=1`、`activeCount=1`、`harvestingCount=0`、`allOutOfRange=true`、`reasonKey=all_miners_out_of_range`。

候选扫描显示半径 8 内最近候选仍未进入资源外环：最佳候选 `5,3` 指向 `行星-1 / gas`，`distance=323.51`、`range=206.29`、`gap=118`、`inRange=false`，且 `placement.reasonKey=no_neighbor`。实际矿站 `-1,1` 在 10 秒等待后最近 `行星-1 / gas`，`distance=463.59`、`range=206.29`、`gap=258`、`inRange=false`；45 秒补资源窗口后 gap 变为 `318`，仍 `inRange=false`。

10 秒等待窗口资源从 `metal 94.59 / ore 47.63 / gas 39.35 / plasma 8.78` 到 `metal 103.09 / ore 49.43 / gas 40.55 / plasma 9.03`。45 秒补资源窗口资源从 `metal 60.09 / ore 37.43 / gas 40.55 / plasma 9.03` 到 `metal 108.35 / ore 45.53 / gas 47.46 / plasma 10.16`。由于 `harvestingCount=0` 且 `target=null`，这些增长不能归因于采矿。

## Toast / StatusAlerts

- `repair` 成功 toast：`维修站已建造。`
- 采矿状态：`采矿站待命：距「行星-1」还差 290，请进入彩色外环`、`采矿站未覆盖资源`。
- 建造模式提示均保留：`点网格或 Enter 放置；Esc / 「指针/航行」退出`。
- 后续提示观察：repair 建成后仍显示下一座 `repair` 的资源不足提示，需在 #5 判断是否会被玩家误读为刚才建造失败。

## PM 结论

- #3 `repair` 建成路径证据：通过。v0.18 的 `repair` 资源不足失败点在本轮通过补资源等待关闭。
- #4 采矿覆盖/收益证据：证据完成但收益未闭环。当前路径证明矿站未覆盖资源外环，等待窗口内资源增长不可归因于采矿。
- 自然交火、战后复盘和 Meta 回流仍建议阻塞，直到 `mining` 覆盖外环并产生可归因收益，或制作人接受“采矿提示足够清楚但收益路径继续补证”的阶段性结论。
- 未覆盖：真实人工鼠标、真实触控、采矿收益成功路径、原路径 #6 完整复测、专项审核。
