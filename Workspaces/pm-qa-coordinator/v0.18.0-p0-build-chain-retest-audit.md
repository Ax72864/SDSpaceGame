# v0.18.0-p0-build-chain-retest #7 专项审核记录

## 基本信息

- 任务：#7 专项审核 + 不回潮回归 + PM 汇总。
- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 +08:00。
- 审核输入：
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-kanban.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-coordinate-diagnostics.md`
  - `Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-after-diagnostics.md`
  - `Game/game.js` 中 v0.18 新增 helper 与周边 renderer/build 逻辑。

## 审核结论

- 技术边界：通过。
- 不回潮回归：通过。
- PM 证据表达：准确，未把辅助输入或只读 helper 包装成人类体验通过。
- 发布闸门：不通过。`repair` 未建成、采矿收益未形成、真实人工鼠标/触控未覆盖，自然交火与 Meta 回流仍阻塞，不能进入发布准备。

## 技术边界核对

- `Game/game.js` 的 v0.18 改动仅追加 `Renderer.worldToScreen` 与 4 个 `window.__gameTest.playtest` 坐标诊断 helper：
  - `getGridScreenPoint`
  - `getGridInteractionDiagnostics`
  - `getPointerGridDiagnostics`
  - `compareGridClickAlignment`
- Helper 均为只读诊断：读取 grid/world/screen/camera/viewport/placement/cell 摘要并通过 `safeDeepCloneForTest` 返回，不写 state/storage。
- Helper 未调用 `buildAt`，未直接修改设施、资源、存档或运行状态。
- 未发现本轮 helper 改动触及建造合法性、资源成本、设施数值、RNG、存档、`SAVE_KEY`、meta schema、战斗、敌人、星系或 Meta 规则。

## 证据口径核对

- `power` 替换链路通过：已记录 `frame -> power`，坐标对齐 `aligned=true`、`distance=0`。
- `mining` 替换链路通过：已记录 `frame -> mining`，但采矿站未覆盖资源外环，等待后 `harvestingCount=0`，采矿收益未形成。
- `turret` 替换链路通过：已记录 `frame -> turret`，坐标对齐 `aligned=true`、`distance=0`。
- `repair` 未通过：已扩出目标 `frame`，但因资源不足未替换为维修站。
- 真实人工鼠标/触控未覆盖；可见 Chrome + CDP + PointerEvent 只能作为浏览器级/辅助输入证据。
- Runtime errors、console errors、unhandled rejections 均记录为 0；临时 profile storage 摘要已记录。

## 问题分级

- S0：0。
- S1：3。
  - `repair` 未建成。
  - 采矿收益未形成。
  - 真实人工输入未覆盖。
- S2：2。
  - CDP/helper 不能替代人类体验。
  - 坐标 helper 在边缘格、DPR、布局或焦点变化下仍有风险。
- S3：3。
  - 文档需持续区分自动检查、浏览器级检查、人工体验和未覆盖路径。
  - 证据需持续区分“设施替换链路通过”和“采矿/维修/发布闸门通过”。
  - 后续制作人决定前需继续清楚记录自然交火与 Meta 仍阻塞。

## 不回潮回归

- `frame` 扩建仍按现有合法空邻格规则执行，未放宽为任意格建造。
- 非 `frame` 设施仍必须替换已有空 `frame`，未改为直接在空邻格建造。
- 资源成本、设施数值、电力消耗、采矿范围、维修成本和 `repair` 资源门槛未因本轮 helper 改动变化。
- 未新增依赖，未发布、未打 tag、未归档 `Releases/`。
- `42.md` 为既有 dirty 文件，本轮审核不得提交。

## PM 接受建议

本阶段可接受的结论是：坐标诊断 helper 的技术边界与不回潮回归通过，且 `power` / `mining` / `turret` 替换链路可作为浏览器级辅助输入证据成立。完整 v0.18 P0 建造链仍未闭环，原因是 `repair`、采矿收益和真实人工输入缺证；建议 #8 制作人继续保持发布闸门关闭，并决定下一步是继续补证还是调整首局目标路径。
