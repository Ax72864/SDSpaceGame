# v0.18.0-p0-build-chain-retest #5 坐标诊断后复测证据

## 基本信息

- 任务：#5 T3 原失败路径复测与采矿链证据。
- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 00:41 +08:00。
- 环境：Windows 10.0.26200；可见 Chrome 148.0.7778.217；CDP 端口 `9356`。
- 页面入口：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 视口：`1387x807`，DPR `2`。
- 存档策略：临时 Chrome profile；`localStorageLength=0`、`sessionStorageLength=0`，未删除、清空或提交用户真实存档。
- 输入方式：可见 Chrome + CDP `Runtime.evaluate` 向 `canvas#game` 派发 `PointerEvent(pointerdown/pointerup)`，坐标来自 `getGridScreenPoint`；未调用 `buildAt`，未使用 helper 直接修改 state。
- 精简 JSON：`Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-after-diagnostics-summary.json`。

## PM 结论

#5 证明 #4 新增坐标 helper 能关闭“目标 gridKey 与命中 gridKey 不一致”的原取证失败：本轮每次点击前 `compareGridClickAlignment.aligned=true`，对齐距离约 `4.89e-13`，`power`、`mining`、`turret` 均按“先建 frame，再替换同一 frame”为对应设施。

但本轮不能判定完整 P0 建造链完全通过：`repair` 步骤已在 `0,-2` 扩出 frame，但替换为维修站时金属不足（need 44 / have 13 / gap 31），未建成 `repair`。采矿链也未闭环：`mining x1` 建成于 `-1,1`，但位置诊断显示距 ore 资源外环仍差约 `236`，等待窗口前后资源没有增长，且该 CDP PointerEvent 会话内 simulation time 未推进，不能作为采矿收益通过证据。

## 分项证据

| 步骤 | 目标格 | 坐标 clientX/Y | 对齐 | facility 前后 | 资源变化 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| `frame -> power` | `-1,-1` | `346.75 / 274.33` | aligned=true，pointerGridKey=`-1,-1` | 空格 -> `frame` -> `power` | metal `130.04 -> 122.04 -> 100.04`，ore `60.01 -> 60.01 -> 52.01` | 通过 |
| `frame -> mining` | `-1,1` | `346.75 / 304.33` | aligned=true，pointerGridKey=`-1,1` | 空格 -> `frame` -> `mining` | metal `100.04 -> 92.04 -> 64.04`，ore `52.01 -> 52.01 -> 42.01` | 建造通过，采矿收益未通过 |
| `frame -> turret` | `-2,0` | `331.75 / 289.33` | aligned=true，pointerGridKey=`-2,0` | 空格 -> `frame` -> `turret` | metal `64.04 -> 56.04 -> 21.04`，ore `42.01 -> 42.01 -> 30.01` | 通过 |
| `frame -> repair` | `0,-2` | `361.75 / 259.33` | aligned=true，pointerGridKey=`0,-2` | 空格 -> `frame`，替换失败后仍为 `frame` | metal `21.04 -> 13.04 -> 13.04` | 未通过，金属不足 |

## 采矿链观察

- `mining` 建成证据：最终设施计数包含 `mining: 1`，目标格 `-1,1` 的 cell facility 为 `mining`。
- 资源外环覆盖：建造前 placement rangeHint 显示最近 ore 资源距离约 `472.99`，采矿范围约 `237.49`，gap 约 `236`，`inRange=false`。
- 等待窗口：等待前资源 `metal 13.04 / ore 30.01 / gas 35.01 / plasma 8`；等待后资源相同。
- 采矿收益结论：未形成收益证据。原因同时包含矿站未覆盖资源外环，以及本次 CDP PointerEvent 会话内 simulation time 未推进。

## Runtime / Storage

- Runtime errors：0。
- Unhandled rejections：0。
- `localStorageLength=0`，`sessionStorageLength=0`。
- 未新增依赖，未修改 `Game/`、`42.md`、`Docs/`、`.cursor/`、`Releases/`、发布/tag 或存档结构。

## 覆盖范围与边界

- 自动检查：使用 #4 只读 helper 采集 `getGridScreenPoint`、`getGridInteractionDiagnostics`、`compareGridClickAlignment`。
- 浏览器级检查：可见 Chrome + CDP，临时 profile，canvas 事件链路。
- 人工体验：未覆盖真实手持鼠标，也未覆盖真实触控。
- 证据边界：PointerEvent 派发可证明 canvas 事件链路和 grid 对齐，但不能等同人工手感通过；物理 CDP click 仍受页面覆盖层影响，曾出现坐标对齐但点击落到 UI 面板的问题。

## 后续判断

- #5 建造替换失败路径：部分关闭。`power/mining/turret` 已证明可通过坐标校准命中同一 frame 并替换；`repair` 因资源不足仍未闭环。
- #5 采矿链：未关闭。已有 `mining x1` 建造证据，但未覆盖资源外环且无资源增长证据。
- #6 交火/Meta：建议继续阻塞。建造链虽有明显进展，但采矿收益、维修站完整建成、真实人工输入仍缺证据，不建议解锁自然交火/Meta 发布闸门。
