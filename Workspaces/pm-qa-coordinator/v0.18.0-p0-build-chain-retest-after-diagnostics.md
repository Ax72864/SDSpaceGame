# v0.18.0-p0-build-chain-retest #5 坐标诊断后复测证据

## 基本信息

- 任务：#5 T3 原失败路径复测与采矿链证据。
- 记录人：`/pm-qa-coordinator`。
- 时间：2026-06-10 00:48 +08:00。
- 环境：Windows 10.0.26200；可见 Chrome；CDP 端口 `9370`。
- 页面入口：`file:///D:/work/project/agentgame/Game/index.html?playtest=1`。
- 视口：`1387x1007`，DPR `2`。
- 存档策略：临时 Chrome profile `agentgame-v018-p0-profile-20260610-004830-9370`；`localStorageLength=0`、`sessionStorageLength=0`，未删除、清空或提交用户真实存档。
- 输入方式：可见 Chrome + CDP 热键选择建造卡；`Runtime.evaluate` 向 `canvas#game` 派发 `PointerEvent(pointermove/pointerdown/pointerup)`，坐标来自 `getGridScreenPoint`；未调用 `buildAt`，未使用 helper 直接修改 state。
- 精简 JSON：`Workspaces/pm-qa-coordinator/v0.18.0-p0-build-chain-retest-after-diagnostics-20260610-004847-summary.json`。

## PM 结论

#5 复测证明 #4 新增只读坐标 helper 能关闭“声明目标 `gridKey` 与 canvas 命中 `gridKey` 不一致”的原失败主因：本轮有效证据中每次 canvas PointerEvent 前后 `compareGridClickAlignment.aligned=true`，`screenDelta.distance=0`，`power`、`mining`、`turret` 均按“先扩 `frame`，再替换同一 `frame`”完成。

完整 P0 建造链仍不能判定完全通过：`repair` 步骤已在 `0,-2` 扩出 `frame`，但替换为维修站时资源不足（metal need 44 / have 23 / gap 21，且需要 plasma 6），未建成 `repair`。采矿链也未闭环：`mining x1` 建成于 `-1,1`，但未覆盖资源外环，等待 10 秒后只看到基础资源自然变化，未形成采矿收获证据。

## 分项证据

| 步骤 | 目标格 | 设施点击 clientX/Y | 对齐 | facility 前后 | 资源变化 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| `frame -> power` | `-1,-1` | `660.70 / 498.38` | aligned=true，distance=0 | 空格 -> `frame` -> `power` | metal `131.05 -> 123.05 -> 101.19`，ore `60.22 -> 60.22 -> 52.25` | 通过 |
| `frame -> mining` | `-1,1` | `695.91 / 482.31` | aligned=true，distance=0 | 空格 -> `frame` -> `mining` | metal `101.19 -> 93.32 -> 65.46`，ore `52.25 -> 52.28 -> 42.31` | 建造通过，采矿收益未通过 |
| `frame -> turret` | `-2,0` | `678.03 / 529.20` | aligned=true，distance=0 | 空格 -> `frame` -> `turret` | metal `73.96 -> 66.23 -> 31.24`，ore `44.11 -> 44.17 -> 32.17` | 通过 |
| `frame -> repair` | `0,-2` | `675.68 / 527.63` | aligned=true，distance=0 | 空格 -> `frame`，替换失败后仍为 `frame` | metal `31.24 -> 23.40 -> 23.53` | 未通过，资源不足 |

## 采矿链观察

- `mining` 建成证据：最终设施计数包含 `mining: 1`，目标格 `-1,1` 的 cell facility 为 `mining`。
- 资源外环覆盖：建造前 placement rangeHint 显示最近资源 `小行星-3 / gas`，距离 `471.88`，采矿范围 `156.77`，gap `316`，`inRange=false`。
- 等待窗口：等待 10 秒，`time 1.72 -> 11.72`；资源 `metal 65.46 -> 73.96`、`ore 42.31 -> 44.11`、`gas 35.21 -> 36.41`、`plasma 8.04 -> 8.29`。
- 采矿收益结论：未形成收益证据。等待后 `getResourceReachability` 显示 `minerCount=1`、`harvestingCount=0`、`allOutOfRange=true`、`reasonKey=all_miners_out_of_range`，资源变化不能归因于采矿。

## Runtime / Storage

- Runtime errors：0。
- Console errors：0。
- Unhandled rejections：0。
- `localStorageLength=0`，`sessionStorageLength=0`。
- 未新增依赖，未修改 `Game/`、`42.md`、`Docs/`、`.cursor/`、`Releases/`、发布/tag 或存档结构。

## 覆盖范围与边界

- 自动检查：使用 #4 只读 helper 采集 `getGridScreenPoint`、`getGridInteractionDiagnostics`、`getPointerGridDiagnostics`、`compareGridClickAlignment`。
- 浏览器级检查：可见 Chrome + CDP，临时 profile，canvas PointerEvent 事件链路。
- 人工体验：未覆盖真实手持鼠标，也未覆盖真实触控。
- 未计入通过的尝试：同轮先尝试过物理 CDP `Input.dispatchMouseEvent`，点击前 helper 对齐但实际设施落点仍漂移到其他格，作为原失败路径风险复现，不计入通过证据。
- 证据边界：PointerEvent 派发可证明 canvas 事件链路和 grid 对齐，但不能等同人工手感通过；物理 CDP click 仍受页面覆盖层、相机移动或事件目标影响。

## 后续判断

- #5 建造替换失败路径：部分关闭。`power/mining/turret` 已证明可通过坐标校准命中同一 frame 并替换；`repair` 因资源不足仍未闭环。
- #5 采矿链：未关闭。已有 `mining x1` 建造证据，但未覆盖资源外环且无采矿增长证据。
- #6 交火/Meta：建议继续阻塞。采矿收益、维修站完整建成、真实人工输入仍缺证据，不建议解锁自然交火/Meta 发布闸门。
