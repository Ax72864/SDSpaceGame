---
description: Git 提交推送、历史保护、版本发布和 Releases 归档规则
alwaysApply: true
---

# Git 与发布规则

## 提交与推送

- 版本管理使用 Git，当前仓库已初始化。
- 每个成员完成自己的任务并通过必要自检后，应及时 commit 并 push。
- 提交信息使用中文简洁描述，例如 `完成舰船移动原型`。
- push 失败时允许安全重试；短时间内持续失败时，保留本地 commit、记录未上传状态，并允许下一次提交时一并 push，优先保证开发进度。
- commit 前必须检查状态和变更范围，避免提交密钥、凭据、临时缓存、无关文件或危险产物。

## 禁止操作

除非用户明确确认，禁止执行：

- `git push --force` 或任何 force push。
- `git reset --hard`、`git clean`、checkout 覆盖等会丢失工作的命令。
- 改写已 push 的提交历史，包括 rebase 或 amend 已推送提交。
- 删除发布 tag、发布分支、发布目录或已归档发布产物。

## 版本发布

- 版本边界由制作人提出方向，PM 根据任务量决定。
- 重要可玩里程碑或关键体验变化触发发布；包括新核心循环可玩、核心体验明显变化、垂直切片完成，或制作人认为值得永久保留的体验节点。
- 发布版本由制作人最终批准。
- 没有明确构建流程时，先由 `/it-engineer` 配置构建环境和最小构建流程。
- 发布版本必须创建 Git tag，版本号使用混合命名，例如 `v0.1.0-prototype`。
- Windows 可运行发布产物归档到 `Releases/<version>/`，并直接提交进 Git。

## 发布目录内容

`Releases/<version>/` 至少包含：

- 可直接运行的游戏构建。
- 版本说明和主要变更。
- 对应 commit、tag、构建时间和构建平台信息。
- 已知问题和后续优化方向。
